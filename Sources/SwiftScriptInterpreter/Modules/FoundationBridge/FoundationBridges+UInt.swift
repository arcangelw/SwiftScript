// AUTO-GENERATED by BridgeGeneratorTool. Do not edit by hand.
// Regenerate with: bash Tools/regen-foundation-bridge.sh
import Foundation
import ShellKit
#if canImport(FoundationNetworking)
import FoundationNetworking
#endif

#if canImport(Darwin)
extension FoundationBridges {
    nonisolated(unsafe) static let uInt: [String: Bridge] = [
    "init UInt()": .`init` { args in
        guard args.count == 0 else {
            throw RuntimeError.invalid("init UInt(): expected 0 argument(s), got \(args.count)")
        }
        return try boxUnsignedAsInt(UInt())
    },
    "init UInt(_:)": .`init` { args in
        guard args.count == 1 else {
            throw RuntimeError.invalid("init UInt(_:): expected 1 argument(s), got \(args.count)")
        }
        if let _v = UInt(try unboxString(args[0])) {
            return .optional(try boxUnsignedAsInt(_v))
        }
        return .optional(nil)
    },
    "init UInt(integerLiteral:)": .`init` { args in
        guard args.count == 1 else {
            throw RuntimeError.invalid("init UInt(integerLiteral:): expected 1 argument(s), got \(args.count)")
        }
        return try boxUnsignedAsInt(UInt(integerLiteral: try toUInt(args[0])))
    },
    "init UInt(littleEndian:)": .`init` { args in
        guard args.count == 1 else {
            throw RuntimeError.invalid("init UInt(littleEndian:): expected 1 argument(s), got \(args.count)")
        }
        return try boxUnsignedAsInt(UInt(littleEndian: try toUInt(args[0])))
    },
    "init UInt(bigEndian:)": .`init` { args in
        guard args.count == 1 else {
            throw RuntimeError.invalid("init UInt(bigEndian:): expected 1 argument(s), got \(args.count)")
        }
        return try boxUnsignedAsInt(UInt(bigEndian: try toUInt(args[0])))
    },
    "init UInt(bitPattern:)": .`init` { args in
        guard args.count == 1 else {
            throw RuntimeError.invalid("init UInt(bitPattern:): expected 1 argument(s), got \(args.count)")
        }
        return try boxUnsignedAsInt(UInt(bitPattern: try unboxOptionalValue(args[0]).map { try unboxOpaque($0, as: OpaquePointer.self, typeName: "OpaquePointer") }))
    },
    "init UInt(exactly:)": .`init` { args in
        guard args.count == 1 else {
            throw RuntimeError.invalid("init UInt(exactly:): expected 1 argument(s), got \(args.count)")
        }
        if let _v = UInt(exactly: try toFloat(args[0])) {
            return .optional(try boxUnsignedAsInt(_v))
        }
        return .optional(nil)
    },
    "var UInt.description: String": .computed { receiver in
        let recv: UInt = try toUInt(receiver)
        return .string(recv.description)
    },
    "var UInt.bitWidth: Int": .computed { receiver in
        let recv: UInt = try toUInt(receiver)
        return .int(recv.bitWidth)
    },
    "var UInt.littleEndian: UInt": .computed { receiver in
        let recv: UInt = try toUInt(receiver)
        return try boxUnsignedAsInt(recv.littleEndian)
    },
    "var UInt.bigEndian: UInt": .computed { receiver in
        let recv: UInt = try toUInt(receiver)
        return try boxUnsignedAsInt(recv.bigEndian)
    },
    "var UInt.magnitude: UInt": .computed { receiver in
        let recv: UInt = try toUInt(receiver)
        return try boxUnsignedAsInt(recv.magnitude)
    },
    "static let UInt.isSigned": .staticValue(.bool(UInt.isSigned)),
    "func UInt.formatted()": .method { receiver, args in
        guard args.count == 0 else {
            throw RuntimeError.invalid("UInt.formatted: expected 0 argument(s), got \(args.count)")
        }
        let recv: UInt = try toUInt(receiver)
        return .string(recv.formatted())
    },
    "func UInt.quotientAndRemainder(dividingBy:)": .method { receiver, args in
        guard args.count == 1 else {
            throw RuntimeError.invalid("UInt.quotientAndRemainder: expected 1 argument(s), got \(args.count)")
        }
        let recv: UInt = try toUInt(receiver)
        let _t = recv.quotientAndRemainder(dividingBy: try toUInt(args[0]))
        return .tuple([try boxUnsignedAsInt(_t.0), try boxUnsignedAsInt(_t.1)])
    },
    "func UInt.quotientAndRemainder()": .method { receiver, args in
        guard args.count == 1 else {
            throw RuntimeError.invalid("UInt.quotientAndRemainder: expected 1 argument(s), got \(args.count)")
        }
        let recv: UInt = try toUInt(receiver)
        let _t = recv.quotientAndRemainder(dividingBy: try toUInt(args[0]))
        return .tuple([try boxUnsignedAsInt(_t.0), try boxUnsignedAsInt(_t.1)])
    },
    "func UInt.isMultiple(of:)": .method { receiver, args in
        guard args.count == 1 else {
            throw RuntimeError.invalid("UInt.isMultiple: expected 1 argument(s), got \(args.count)")
        }
        let recv: UInt = try toUInt(receiver)
        return .bool(recv.isMultiple(of: try toUInt(args[0])))
    },
    "func UInt.isMultiple()": .method { receiver, args in
        guard args.count == 1 else {
            throw RuntimeError.invalid("UInt.isMultiple: expected 1 argument(s), got \(args.count)")
        }
        let recv: UInt = try toUInt(receiver)
        return .bool(recv.isMultiple(of: try toUInt(args[0])))
    },
    "func UInt.distance(to:)": .method { receiver, args in
        guard args.count == 1 else {
            throw RuntimeError.invalid("UInt.distance: expected 1 argument(s), got \(args.count)")
        }
        let recv: UInt = try toUInt(receiver)
        return .int(recv.distance(to: try toUInt(args[0])))
    },
    "func UInt.distance()": .method { receiver, args in
        guard args.count == 1 else {
            throw RuntimeError.invalid("UInt.distance: expected 1 argument(s), got \(args.count)")
        }
        let recv: UInt = try toUInt(receiver)
        return .int(recv.distance(to: try toUInt(args[0])))
    },
    "func UInt.advanced(by:)": .method { receiver, args in
        guard args.count == 1 else {
            throw RuntimeError.invalid("UInt.advanced: expected 1 argument(s), got \(args.count)")
        }
        let recv: UInt = try toUInt(receiver)
        return try boxUnsignedAsInt(recv.advanced(by: try unboxInt(args[0])))
    },
    "func UInt.advanced()": .method { receiver, args in
        guard args.count == 1 else {
            throw RuntimeError.invalid("UInt.advanced: expected 1 argument(s), got \(args.count)")
        }
        let recv: UInt = try toUInt(receiver)
        return try boxUnsignedAsInt(recv.advanced(by: try unboxInt(args[0])))
    },
    "static let UInt.bitWidth": .staticValue(.int(UInt.bitWidth)),
    "var UInt.leadingZeroBitCount: Int": .computed { receiver in
        let recv: UInt = try toUInt(receiver)
        return .int(recv.leadingZeroBitCount)
    },
    "var UInt.trailingZeroBitCount: Int": .computed { receiver in
        let recv: UInt = try toUInt(receiver)
        return .int(recv.trailingZeroBitCount)
    },
    "var UInt.nonzeroBitCount: Int": .computed { receiver in
        let recv: UInt = try toUInt(receiver)
        return .int(recv.nonzeroBitCount)
    },
    "var UInt.byteSwapped: UInt": .computed { receiver in
        let recv: UInt = try toUInt(receiver)
        return try boxUnsignedAsInt(recv.byteSwapped)
    },
    "func UInt.signum()": .method { receiver, args in
        guard args.count == 0 else {
            throw RuntimeError.invalid("UInt.signum: expected 0 argument(s), got \(args.count)")
        }
        let recv: UInt = try toUInt(receiver)
        return try boxUnsignedAsInt(recv.signum())
    },
    "var UInt.hashValue: Int": .computed { receiver in
        let recv: UInt = try toUInt(receiver)
        return .int(recv.hashValue)
    },
    "func UInt.addingReportingOverflow(_:)": .method { receiver, args in
        guard args.count == 1 else {
            throw RuntimeError.invalid("UInt.addingReportingOverflow: expected 1 argument(s), got \(args.count)")
        }
        let recv: UInt = try toUInt(receiver)
        let _t = recv.addingReportingOverflow(try toUInt(args[0]))
        return .tuple([try boxUnsignedAsInt(_t.0), .bool(_t.1)])
    },
    "func UInt.addingReportingOverflow()": .method { receiver, args in
        guard args.count == 1 else {
            throw RuntimeError.invalid("UInt.addingReportingOverflow: expected 1 argument(s), got \(args.count)")
        }
        let recv: UInt = try toUInt(receiver)
        let _t = recv.addingReportingOverflow(try toUInt(args[0]))
        return .tuple([try boxUnsignedAsInt(_t.0), .bool(_t.1)])
    },
    "func UInt.subtractingReportingOverflow(_:)": .method { receiver, args in
        guard args.count == 1 else {
            throw RuntimeError.invalid("UInt.subtractingReportingOverflow: expected 1 argument(s), got \(args.count)")
        }
        let recv: UInt = try toUInt(receiver)
        let _t = recv.subtractingReportingOverflow(try toUInt(args[0]))
        return .tuple([try boxUnsignedAsInt(_t.0), .bool(_t.1)])
    },
    "func UInt.subtractingReportingOverflow()": .method { receiver, args in
        guard args.count == 1 else {
            throw RuntimeError.invalid("UInt.subtractingReportingOverflow: expected 1 argument(s), got \(args.count)")
        }
        let recv: UInt = try toUInt(receiver)
        let _t = recv.subtractingReportingOverflow(try toUInt(args[0]))
        return .tuple([try boxUnsignedAsInt(_t.0), .bool(_t.1)])
    },
    "func UInt.multipliedReportingOverflow(by:)": .method { receiver, args in
        guard args.count == 1 else {
            throw RuntimeError.invalid("UInt.multipliedReportingOverflow: expected 1 argument(s), got \(args.count)")
        }
        let recv: UInt = try toUInt(receiver)
        let _t = recv.multipliedReportingOverflow(by: try toUInt(args[0]))
        return .tuple([try boxUnsignedAsInt(_t.0), .bool(_t.1)])
    },
    "func UInt.multipliedReportingOverflow()": .method { receiver, args in
        guard args.count == 1 else {
            throw RuntimeError.invalid("UInt.multipliedReportingOverflow: expected 1 argument(s), got \(args.count)")
        }
        let recv: UInt = try toUInt(receiver)
        let _t = recv.multipliedReportingOverflow(by: try toUInt(args[0]))
        return .tuple([try boxUnsignedAsInt(_t.0), .bool(_t.1)])
    },
    "func UInt.dividedReportingOverflow(by:)": .method { receiver, args in
        guard args.count == 1 else {
            throw RuntimeError.invalid("UInt.dividedReportingOverflow: expected 1 argument(s), got \(args.count)")
        }
        let recv: UInt = try toUInt(receiver)
        let _t = recv.dividedReportingOverflow(by: try toUInt(args[0]))
        return .tuple([try boxUnsignedAsInt(_t.0), .bool(_t.1)])
    },
    "func UInt.dividedReportingOverflow()": .method { receiver, args in
        guard args.count == 1 else {
            throw RuntimeError.invalid("UInt.dividedReportingOverflow: expected 1 argument(s), got \(args.count)")
        }
        let recv: UInt = try toUInt(receiver)
        let _t = recv.dividedReportingOverflow(by: try toUInt(args[0]))
        return .tuple([try boxUnsignedAsInt(_t.0), .bool(_t.1)])
    },
    "func UInt.remainderReportingOverflow(dividingBy:)": .method { receiver, args in
        guard args.count == 1 else {
            throw RuntimeError.invalid("UInt.remainderReportingOverflow: expected 1 argument(s), got \(args.count)")
        }
        let recv: UInt = try toUInt(receiver)
        let _t = recv.remainderReportingOverflow(dividingBy: try toUInt(args[0]))
        return .tuple([try boxUnsignedAsInt(_t.0), .bool(_t.1)])
    },
    "func UInt.remainderReportingOverflow()": .method { receiver, args in
        guard args.count == 1 else {
            throw RuntimeError.invalid("UInt.remainderReportingOverflow: expected 1 argument(s), got \(args.count)")
        }
        let recv: UInt = try toUInt(receiver)
        let _t = recv.remainderReportingOverflow(dividingBy: try toUInt(args[0]))
        return .tuple([try boxUnsignedAsInt(_t.0), .bool(_t.1)])
    },
    ]
}
#else
extension FoundationBridges {
    nonisolated(unsafe) static let uInt: [String: Bridge] = [:]
}
#endif
