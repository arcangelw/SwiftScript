// AUTO-GENERATED by BridgeGeneratorTool. Do not edit by hand.
// Regenerate with: bash Tools/regen-foundation-bridge.sh
import Foundation
import ShellKit
#if canImport(FoundationNetworking)
import FoundationNetworking
#endif

extension FoundationBridges {
    nonisolated(unsafe) static let dateFormatter: [String: Bridge] = [
    "var DateFormatter.dateFormat: String": .computed { receiver in
        let recv: DateFormatter = try unboxOpaque(receiver, as: DateFormatter.self, typeName: "DateFormatter")
        return .string(recv.dateFormat)
    },
        "set var DateFormatter.dateFormat: String": .setter { receiver, newValue in
            let recv: DateFormatter = try unboxOpaque(receiver, as: DateFormatter.self, typeName: "DateFormatter")
            recv.dateFormat = try unboxString(unwrapForSetter(newValue))
        },
    "var DateFormatter.locale: Locale": .computed { receiver in
        let recv: DateFormatter = try unboxOpaque(receiver, as: DateFormatter.self, typeName: "DateFormatter")
        return boxOpaque(recv.locale, typeName: "Locale")
    },
        "set var DateFormatter.locale: Locale": .setter { receiver, newValue in
            let recv: DateFormatter = try unboxOpaque(receiver, as: DateFormatter.self, typeName: "DateFormatter")
            recv.locale = try unboxOpaque(unwrapForSetter(newValue), as: Locale.self, typeName: "Locale")
        },
    "var DateFormatter.generatesCalendarDates: Bool": .computed { receiver in
        let recv: DateFormatter = try unboxOpaque(receiver, as: DateFormatter.self, typeName: "DateFormatter")
        return .bool(recv.generatesCalendarDates)
    },
        "set var DateFormatter.generatesCalendarDates: Bool": .setter { receiver, newValue in
            let recv: DateFormatter = try unboxOpaque(receiver, as: DateFormatter.self, typeName: "DateFormatter")
            recv.generatesCalendarDates = try unboxBool(unwrapForSetter(newValue))
        },
    "var DateFormatter.timeZone: TimeZone": .computed { receiver in
        let recv: DateFormatter = try unboxOpaque(receiver, as: DateFormatter.self, typeName: "DateFormatter")
        return boxOpaque(recv.timeZone, typeName: "TimeZone")
    },
        "set var DateFormatter.timeZone: TimeZone": .setter { receiver, newValue in
            let recv: DateFormatter = try unboxOpaque(receiver, as: DateFormatter.self, typeName: "DateFormatter")
            recv.timeZone = try unboxOpaque(unwrapForSetter(newValue), as: TimeZone.self, typeName: "TimeZone")
        },
    "var DateFormatter.calendar: Calendar": .computed { receiver in
        let recv: DateFormatter = try unboxOpaque(receiver, as: DateFormatter.self, typeName: "DateFormatter")
        return boxOpaque(recv.calendar, typeName: "Calendar")
    },
        "set var DateFormatter.calendar: Calendar": .setter { receiver, newValue in
            let recv: DateFormatter = try unboxOpaque(receiver, as: DateFormatter.self, typeName: "DateFormatter")
            recv.calendar = try unboxOpaque(unwrapForSetter(newValue), as: Calendar.self, typeName: "Calendar")
        },
    "var DateFormatter.isLenient: Bool": .computed { receiver in
        let recv: DateFormatter = try unboxOpaque(receiver, as: DateFormatter.self, typeName: "DateFormatter")
        return .bool(recv.isLenient)
    },
        "set var DateFormatter.isLenient: Bool": .setter { receiver, newValue in
            let recv: DateFormatter = try unboxOpaque(receiver, as: DateFormatter.self, typeName: "DateFormatter")
            recv.isLenient = try unboxBool(unwrapForSetter(newValue))
        },
    "var DateFormatter.twoDigitStartDate: Date?": .computed { receiver in
        let recv: DateFormatter = try unboxOpaque(receiver, as: DateFormatter.self, typeName: "DateFormatter")
        if let _v = recv.twoDigitStartDate {
            return .optional(boxOpaque(_v, typeName: "Date"))
        }
        return .optional(nil)
    },
        "set var DateFormatter.twoDigitStartDate: Date?": .setter { receiver, newValue in
            let recv: DateFormatter = try unboxOpaque(receiver, as: DateFormatter.self, typeName: "DateFormatter")
            recv.twoDigitStartDate = try unboxOptionalValue(unwrapForSetter(newValue)).map { try unboxOpaque($0, as: Date.self, typeName: "Date") }
        },
    "var DateFormatter.defaultDate: Date?": .computed { receiver in
        let recv: DateFormatter = try unboxOpaque(receiver, as: DateFormatter.self, typeName: "DateFormatter")
        if let _v = recv.defaultDate {
            return .optional(boxOpaque(_v, typeName: "Date"))
        }
        return .optional(nil)
    },
        "set var DateFormatter.defaultDate: Date?": .setter { receiver, newValue in
            let recv: DateFormatter = try unboxOpaque(receiver, as: DateFormatter.self, typeName: "DateFormatter")
            recv.defaultDate = try unboxOptionalValue(unwrapForSetter(newValue)).map { try unboxOpaque($0, as: Date.self, typeName: "Date") }
        },
    "var DateFormatter.amSymbol: String": .computed { receiver in
        let recv: DateFormatter = try unboxOpaque(receiver, as: DateFormatter.self, typeName: "DateFormatter")
        return .string(recv.amSymbol)
    },
        "set var DateFormatter.amSymbol: String": .setter { receiver, newValue in
            let recv: DateFormatter = try unboxOpaque(receiver, as: DateFormatter.self, typeName: "DateFormatter")
            recv.amSymbol = try unboxString(unwrapForSetter(newValue))
        },
    "var DateFormatter.pmSymbol: String": .computed { receiver in
        let recv: DateFormatter = try unboxOpaque(receiver, as: DateFormatter.self, typeName: "DateFormatter")
        return .string(recv.pmSymbol)
    },
        "set var DateFormatter.pmSymbol: String": .setter { receiver, newValue in
            let recv: DateFormatter = try unboxOpaque(receiver, as: DateFormatter.self, typeName: "DateFormatter")
            recv.pmSymbol = try unboxString(unwrapForSetter(newValue))
        },
    "var DateFormatter.gregorianStartDate: Date?": .computed { receiver in
        let recv: DateFormatter = try unboxOpaque(receiver, as: DateFormatter.self, typeName: "DateFormatter")
        if let _v = recv.gregorianStartDate {
            return .optional(boxOpaque(_v, typeName: "Date"))
        }
        return .optional(nil)
    },
        "set var DateFormatter.gregorianStartDate: Date?": .setter { receiver, newValue in
            let recv: DateFormatter = try unboxOpaque(receiver, as: DateFormatter.self, typeName: "DateFormatter")
            recv.gregorianStartDate = try unboxOptionalValue(unwrapForSetter(newValue)).map { try unboxOpaque($0, as: Date.self, typeName: "Date") }
        },
    "var DateFormatter.doesRelativeDateFormatting: Bool": .computed { receiver in
        let recv: DateFormatter = try unboxOpaque(receiver, as: DateFormatter.self, typeName: "DateFormatter")
        return .bool(recv.doesRelativeDateFormatting)
    },
        "set var DateFormatter.doesRelativeDateFormatting: Bool": .setter { receiver, newValue in
            let recv: DateFormatter = try unboxOpaque(receiver, as: DateFormatter.self, typeName: "DateFormatter")
            recv.doesRelativeDateFormatting = try unboxBool(unwrapForSetter(newValue))
        },
    "func DateFormatter.string(from:)": .method { receiver, args in
        guard args.count == 1 else {
            throw RuntimeError.invalid("DateFormatter.string: expected 1 argument(s), got \(args.count)")
        }
        let recv: DateFormatter = try unboxOpaque(receiver, as: DateFormatter.self, typeName: "DateFormatter")
        return .string(recv.string(from: try unboxOpaque(args[0], as: Date.self, typeName: "Date")))
    },
    "func DateFormatter.string()": .method { receiver, args in
        guard args.count == 1 else {
            throw RuntimeError.invalid("DateFormatter.string: expected 1 argument(s), got \(args.count)")
        }
        let recv: DateFormatter = try unboxOpaque(receiver, as: DateFormatter.self, typeName: "DateFormatter")
        return .string(recv.string(from: try unboxOpaque(args[0], as: Date.self, typeName: "Date")))
    },
    "func DateFormatter.date(from:)": .method { receiver, args in
        guard args.count == 1 else {
            throw RuntimeError.invalid("DateFormatter.date: expected 1 argument(s), got \(args.count)")
        }
        let recv: DateFormatter = try unboxOpaque(receiver, as: DateFormatter.self, typeName: "DateFormatter")
        if let _v = recv.date(from: try unboxString(args[0])) {
            return .optional(boxOpaque(_v, typeName: "Date"))
        }
        return .optional(nil)
    },
    "func DateFormatter.date()": .method { receiver, args in
        guard args.count == 1 else {
            throw RuntimeError.invalid("DateFormatter.date: expected 1 argument(s), got \(args.count)")
        }
        let recv: DateFormatter = try unboxOpaque(receiver, as: DateFormatter.self, typeName: "DateFormatter")
        if let _v = recv.date(from: try unboxString(args[0])) {
            return .optional(boxOpaque(_v, typeName: "Date"))
        }
        return .optional(nil)
    },
    "func DateFormatter.setLocalizedDateFormatFromTemplate(_:)": .method { receiver, args in
        guard args.count == 1 else {
            throw RuntimeError.invalid("DateFormatter.setLocalizedDateFormatFromTemplate: expected 1 argument(s), got \(args.count)")
        }
        let recv: DateFormatter = try unboxOpaque(receiver, as: DateFormatter.self, typeName: "DateFormatter")
        recv.setLocalizedDateFormatFromTemplate(try unboxString(args[0]))
            return .void
    },
    "func DateFormatter.setLocalizedDateFormatFromTemplate()": .method { receiver, args in
        guard args.count == 1 else {
            throw RuntimeError.invalid("DateFormatter.setLocalizedDateFormatFromTemplate: expected 1 argument(s), got \(args.count)")
        }
        let recv: DateFormatter = try unboxOpaque(receiver, as: DateFormatter.self, typeName: "DateFormatter")
        recv.setLocalizedDateFormatFromTemplate(try unboxString(args[0]))
            return .void
    },
    "static func DateFormatter.dateFormat()": .staticMethod { args in
        guard args.count == 3 else {
            throw RuntimeError.invalid("DateFormatter.dateFormat: expected 3 argument(s), got \(args.count)")
        }
        if let _v = DateFormatter.dateFormat(fromTemplate: try unboxString(args[0]), options: try unboxInt(args[1]), locale: try unboxOptionalValue(args[2]).map { try unboxOpaque($0, as: Locale.self, typeName: "Locale") }) {
            return .optional(.string(_v))
        }
        return .optional(nil)
    },
        "init DateFormatter()": .`init` { args in
            guard args.isEmpty else {
                throw RuntimeError.invalid("init DateFormatter(): expected 0 argument(s), got \(args.count)")
            }
            return boxOpaque(DateFormatter(), typeName: "DateFormatter")
        },
    ]
}
