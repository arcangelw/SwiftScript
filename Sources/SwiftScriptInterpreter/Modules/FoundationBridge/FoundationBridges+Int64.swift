// AUTO-GENERATED by BridgeGeneratorTool. Do not edit by hand.
// Regenerate with: bash Tools/regen-foundation-bridge.sh
import Foundation
import ShellKit
#if canImport(FoundationNetworking)
import FoundationNetworking
#endif

#if canImport(Darwin)
extension FoundationBridges {
    nonisolated(unsafe) static let int64: [String: Bridge] = [
    "init Int64()": .`init` { args in
        guard args.count == 0 else {
            throw RuntimeError.invalid("init Int64(): expected 0 argument(s), got \(args.count)")
        }
        return .int(Int(Int64()))
    },
    "init Int64(_:)": .`init` { args in
        guard args.count == 1 else {
            throw RuntimeError.invalid("init Int64(_:): expected 1 argument(s), got \(args.count)")
        }
        if let _v = Int64(try unboxString(args[0])) {
            return .optional(.int(Int(_v)))
        }
        return .optional(nil)
    },
    "init Int64(integerLiteral:)": .`init` { args in
        guard args.count == 1 else {
            throw RuntimeError.invalid("init Int64(integerLiteral:): expected 1 argument(s), got \(args.count)")
        }
        return .int(Int(Int64(integerLiteral: try toInt64(args[0]))))
    },
    "init Int64(littleEndian:)": .`init` { args in
        guard args.count == 1 else {
            throw RuntimeError.invalid("init Int64(littleEndian:): expected 1 argument(s), got \(args.count)")
        }
        return .int(Int(Int64(littleEndian: try toInt64(args[0]))))
    },
    "init Int64(bigEndian:)": .`init` { args in
        guard args.count == 1 else {
            throw RuntimeError.invalid("init Int64(bigEndian:): expected 1 argument(s), got \(args.count)")
        }
        return .int(Int(Int64(bigEndian: try toInt64(args[0]))))
    },
    "init Int64(bitPattern:)": .`init` { args in
        guard args.count == 1 else {
            throw RuntimeError.invalid("init Int64(bitPattern:): expected 1 argument(s), got \(args.count)")
        }
        return .int(Int(Int64(bitPattern: try toUInt64(args[0]))))
    },
    "init Int64(exactly:)": .`init` { args in
        guard args.count == 1 else {
            throw RuntimeError.invalid("init Int64(exactly:): expected 1 argument(s), got \(args.count)")
        }
        if let _v = Int64(exactly: try toFloat(args[0])) {
            return .optional(.int(Int(_v)))
        }
        return .optional(nil)
    },
    "static let Int64.zero": .staticValue(.int(Int(Int64.zero))),
    "var Int64.description: String": .computed { receiver in
        let recv: Int64 = try toInt64(receiver)
        return .string(recv.description)
    },
    "var Int64.bitWidth: Int": .computed { receiver in
        let recv: Int64 = try toInt64(receiver)
        return .int(recv.bitWidth)
    },
    "var Int64.littleEndian: Int64": .computed { receiver in
        let recv: Int64 = try toInt64(receiver)
        return .int(Int(recv.littleEndian))
    },
    "var Int64.bigEndian: Int64": .computed { receiver in
        let recv: Int64 = try toInt64(receiver)
        return .int(Int(recv.bigEndian))
    },
    "static let Int64.isSigned": .staticValue(.bool(Int64.isSigned)),
    "static let Int64.max": .staticValue(.int(Int(Int64.max))),
    "static let Int64.min": .staticValue(.int(Int(Int64.min))),
    "func Int64.formatted()": .method { receiver, args in
        guard args.count == 0 else {
            throw RuntimeError.invalid("Int64.formatted: expected 0 argument(s), got \(args.count)")
        }
        let recv: Int64 = try toInt64(receiver)
        return .string(recv.formatted())
    },
    "func Int64.quotientAndRemainder(dividingBy:)": .method { receiver, args in
        guard args.count == 1 else {
            throw RuntimeError.invalid("Int64.quotientAndRemainder: expected 1 argument(s), got \(args.count)")
        }
        let recv: Int64 = try toInt64(receiver)
        let _t = recv.quotientAndRemainder(dividingBy: try toInt64(args[0]))
        return .tuple([.int(Int(_t.0)), .int(Int(_t.1))])
    },
    "func Int64.quotientAndRemainder()": .method { receiver, args in
        guard args.count == 1 else {
            throw RuntimeError.invalid("Int64.quotientAndRemainder: expected 1 argument(s), got \(args.count)")
        }
        let recv: Int64 = try toInt64(receiver)
        let _t = recv.quotientAndRemainder(dividingBy: try toInt64(args[0]))
        return .tuple([.int(Int(_t.0)), .int(Int(_t.1))])
    },
    "func Int64.distance(to:)": .method { receiver, args in
        guard args.count == 1 else {
            throw RuntimeError.invalid("Int64.distance: expected 1 argument(s), got \(args.count)")
        }
        let recv: Int64 = try toInt64(receiver)
        return .int(recv.distance(to: try toInt64(args[0])))
    },
    "func Int64.distance()": .method { receiver, args in
        guard args.count == 1 else {
            throw RuntimeError.invalid("Int64.distance: expected 1 argument(s), got \(args.count)")
        }
        let recv: Int64 = try toInt64(receiver)
        return .int(recv.distance(to: try toInt64(args[0])))
    },
    "func Int64.advanced(by:)": .method { receiver, args in
        guard args.count == 1 else {
            throw RuntimeError.invalid("Int64.advanced: expected 1 argument(s), got \(args.count)")
        }
        let recv: Int64 = try toInt64(receiver)
        return .int(Int(recv.advanced(by: try unboxInt(args[0]))))
    },
    "func Int64.advanced()": .method { receiver, args in
        guard args.count == 1 else {
            throw RuntimeError.invalid("Int64.advanced: expected 1 argument(s), got \(args.count)")
        }
        let recv: Int64 = try toInt64(receiver)
        return .int(Int(recv.advanced(by: try unboxInt(args[0]))))
    },
    "func Int64.isMultiple(of:)": .method { receiver, args in
        guard args.count == 1 else {
            throw RuntimeError.invalid("Int64.isMultiple: expected 1 argument(s), got \(args.count)")
        }
        let recv: Int64 = try toInt64(receiver)
        return .bool(recv.isMultiple(of: try toInt64(args[0])))
    },
    "func Int64.isMultiple()": .method { receiver, args in
        guard args.count == 1 else {
            throw RuntimeError.invalid("Int64.isMultiple: expected 1 argument(s), got \(args.count)")
        }
        let recv: Int64 = try toInt64(receiver)
        return .bool(recv.isMultiple(of: try toInt64(args[0])))
    },
    "static let Int64.bitWidth": .staticValue(.int(Int64.bitWidth)),
    "var Int64.leadingZeroBitCount: Int": .computed { receiver in
        let recv: Int64 = try toInt64(receiver)
        return .int(recv.leadingZeroBitCount)
    },
    "var Int64.trailingZeroBitCount: Int": .computed { receiver in
        let recv: Int64 = try toInt64(receiver)
        return .int(recv.trailingZeroBitCount)
    },
    "var Int64.nonzeroBitCount: Int": .computed { receiver in
        let recv: Int64 = try toInt64(receiver)
        return .int(recv.nonzeroBitCount)
    },
    "var Int64.magnitude: UInt64": .computed { receiver in
        let recv: Int64 = try toInt64(receiver)
        return try boxUnsignedAsInt(recv.magnitude)
    },
    "var Int64.byteSwapped: Int64": .computed { receiver in
        let recv: Int64 = try toInt64(receiver)
        return .int(Int(recv.byteSwapped))
    },
    "func Int64.signum()": .method { receiver, args in
        guard args.count == 0 else {
            throw RuntimeError.invalid("Int64.signum: expected 0 argument(s), got \(args.count)")
        }
        let recv: Int64 = try toInt64(receiver)
        return .int(Int(recv.signum()))
    },
    "var Int64.hashValue: Int": .computed { receiver in
        let recv: Int64 = try toInt64(receiver)
        return .int(recv.hashValue)
    },
    "func Int64.addingReportingOverflow(_:)": .method { receiver, args in
        guard args.count == 1 else {
            throw RuntimeError.invalid("Int64.addingReportingOverflow: expected 1 argument(s), got \(args.count)")
        }
        let recv: Int64 = try toInt64(receiver)
        let _t = recv.addingReportingOverflow(try toInt64(args[0]))
        return .tuple([.int(Int(_t.0)), .bool(_t.1)])
    },
    "func Int64.addingReportingOverflow()": .method { receiver, args in
        guard args.count == 1 else {
            throw RuntimeError.invalid("Int64.addingReportingOverflow: expected 1 argument(s), got \(args.count)")
        }
        let recv: Int64 = try toInt64(receiver)
        let _t = recv.addingReportingOverflow(try toInt64(args[0]))
        return .tuple([.int(Int(_t.0)), .bool(_t.1)])
    },
    "func Int64.subtractingReportingOverflow(_:)": .method { receiver, args in
        guard args.count == 1 else {
            throw RuntimeError.invalid("Int64.subtractingReportingOverflow: expected 1 argument(s), got \(args.count)")
        }
        let recv: Int64 = try toInt64(receiver)
        let _t = recv.subtractingReportingOverflow(try toInt64(args[0]))
        return .tuple([.int(Int(_t.0)), .bool(_t.1)])
    },
    "func Int64.subtractingReportingOverflow()": .method { receiver, args in
        guard args.count == 1 else {
            throw RuntimeError.invalid("Int64.subtractingReportingOverflow: expected 1 argument(s), got \(args.count)")
        }
        let recv: Int64 = try toInt64(receiver)
        let _t = recv.subtractingReportingOverflow(try toInt64(args[0]))
        return .tuple([.int(Int(_t.0)), .bool(_t.1)])
    },
    "func Int64.multipliedReportingOverflow(by:)": .method { receiver, args in
        guard args.count == 1 else {
            throw RuntimeError.invalid("Int64.multipliedReportingOverflow: expected 1 argument(s), got \(args.count)")
        }
        let recv: Int64 = try toInt64(receiver)
        let _t = recv.multipliedReportingOverflow(by: try toInt64(args[0]))
        return .tuple([.int(Int(_t.0)), .bool(_t.1)])
    },
    "func Int64.multipliedReportingOverflow()": .method { receiver, args in
        guard args.count == 1 else {
            throw RuntimeError.invalid("Int64.multipliedReportingOverflow: expected 1 argument(s), got \(args.count)")
        }
        let recv: Int64 = try toInt64(receiver)
        let _t = recv.multipliedReportingOverflow(by: try toInt64(args[0]))
        return .tuple([.int(Int(_t.0)), .bool(_t.1)])
    },
    "func Int64.dividedReportingOverflow(by:)": .method { receiver, args in
        guard args.count == 1 else {
            throw RuntimeError.invalid("Int64.dividedReportingOverflow: expected 1 argument(s), got \(args.count)")
        }
        let recv: Int64 = try toInt64(receiver)
        let _t = recv.dividedReportingOverflow(by: try toInt64(args[0]))
        return .tuple([.int(Int(_t.0)), .bool(_t.1)])
    },
    "func Int64.dividedReportingOverflow()": .method { receiver, args in
        guard args.count == 1 else {
            throw RuntimeError.invalid("Int64.dividedReportingOverflow: expected 1 argument(s), got \(args.count)")
        }
        let recv: Int64 = try toInt64(receiver)
        let _t = recv.dividedReportingOverflow(by: try toInt64(args[0]))
        return .tuple([.int(Int(_t.0)), .bool(_t.1)])
    },
    "func Int64.remainderReportingOverflow(dividingBy:)": .method { receiver, args in
        guard args.count == 1 else {
            throw RuntimeError.invalid("Int64.remainderReportingOverflow: expected 1 argument(s), got \(args.count)")
        }
        let recv: Int64 = try toInt64(receiver)
        let _t = recv.remainderReportingOverflow(dividingBy: try toInt64(args[0]))
        return .tuple([.int(Int(_t.0)), .bool(_t.1)])
    },
    "func Int64.remainderReportingOverflow()": .method { receiver, args in
        guard args.count == 1 else {
            throw RuntimeError.invalid("Int64.remainderReportingOverflow: expected 1 argument(s), got \(args.count)")
        }
        let recv: Int64 = try toInt64(receiver)
        let _t = recv.remainderReportingOverflow(dividingBy: try toInt64(args[0]))
        return .tuple([.int(Int(_t.0)), .bool(_t.1)])
    },
    ]
}
#else
extension FoundationBridges {
    nonisolated(unsafe) static let int64: [String: Bridge] = [:]
}
#endif
