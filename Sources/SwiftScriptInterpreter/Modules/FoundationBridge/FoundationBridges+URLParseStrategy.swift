// AUTO-GENERATED by BridgeGeneratorTool. Do not edit by hand.
// Regenerate with: bash Tools/regen-foundation-bridge.sh
import Foundation
import ShellKit
#if canImport(FoundationNetworking)
import FoundationNetworking
#endif

#if canImport(Darwin)
extension FoundationBridges {
    nonisolated(unsafe) static let uRLParseStrategy: [String: Bridge] = [
    "static let URL.ParseStrategy.url": .staticValue(boxOpaque(URL.ParseStrategy.url, typeName: "URL.ParseStrategy")),
    "var URL.ParseStrategy.hashValue: Int": .computed { receiver in
        let recv: URL.ParseStrategy = try unboxOpaque(receiver, as: URL.ParseStrategy.self, typeName: "URL.ParseStrategy")
        return .int(recv.hashValue)
    },
    "func URL.ParseStrategy.scheme()": .method { receiver, args in
        guard args.count == 0 else {
            throw RuntimeError.invalid("URL.ParseStrategy.scheme: expected 0 argument(s), got \(args.count)")
        }
        let recv: URL.ParseStrategy = try unboxOpaque(receiver, as: URL.ParseStrategy.self, typeName: "URL.ParseStrategy")
        return boxOpaque(recv.scheme(), typeName: "URL.ParseStrategy")
    },
    "func URL.ParseStrategy.user()": .method { receiver, args in
        guard args.count == 0 else {
            throw RuntimeError.invalid("URL.ParseStrategy.user: expected 0 argument(s), got \(args.count)")
        }
        let recv: URL.ParseStrategy = try unboxOpaque(receiver, as: URL.ParseStrategy.self, typeName: "URL.ParseStrategy")
        return boxOpaque(recv.user(), typeName: "URL.ParseStrategy")
    },
    "func URL.ParseStrategy.password()": .method { receiver, args in
        guard args.count == 0 else {
            throw RuntimeError.invalid("URL.ParseStrategy.password: expected 0 argument(s), got \(args.count)")
        }
        let recv: URL.ParseStrategy = try unboxOpaque(receiver, as: URL.ParseStrategy.self, typeName: "URL.ParseStrategy")
        return boxOpaque(recv.password(), typeName: "URL.ParseStrategy")
    },
    "func URL.ParseStrategy.host()": .method { receiver, args in
        guard args.count == 0 else {
            throw RuntimeError.invalid("URL.ParseStrategy.host: expected 0 argument(s), got \(args.count)")
        }
        let recv: URL.ParseStrategy = try unboxOpaque(receiver, as: URL.ParseStrategy.self, typeName: "URL.ParseStrategy")
        return boxOpaque(recv.host(), typeName: "URL.ParseStrategy")
    },
    "func URL.ParseStrategy.port()": .method { receiver, args in
        guard args.count == 0 else {
            throw RuntimeError.invalid("URL.ParseStrategy.port: expected 0 argument(s), got \(args.count)")
        }
        let recv: URL.ParseStrategy = try unboxOpaque(receiver, as: URL.ParseStrategy.self, typeName: "URL.ParseStrategy")
        return boxOpaque(recv.port(), typeName: "URL.ParseStrategy")
    },
    "func URL.ParseStrategy.path()": .method { receiver, args in
        guard args.count == 0 else {
            throw RuntimeError.invalid("URL.ParseStrategy.path: expected 0 argument(s), got \(args.count)")
        }
        let recv: URL.ParseStrategy = try unboxOpaque(receiver, as: URL.ParseStrategy.self, typeName: "URL.ParseStrategy")
        return boxOpaque(recv.path(), typeName: "URL.ParseStrategy")
    },
    "func URL.ParseStrategy.query()": .method { receiver, args in
        guard args.count == 0 else {
            throw RuntimeError.invalid("URL.ParseStrategy.query: expected 0 argument(s), got \(args.count)")
        }
        let recv: URL.ParseStrategy = try unboxOpaque(receiver, as: URL.ParseStrategy.self, typeName: "URL.ParseStrategy")
        return boxOpaque(recv.query(), typeName: "URL.ParseStrategy")
    },
    "func URL.ParseStrategy.fragment()": .method { receiver, args in
        guard args.count == 0 else {
            throw RuntimeError.invalid("URL.ParseStrategy.fragment: expected 0 argument(s), got \(args.count)")
        }
        let recv: URL.ParseStrategy = try unboxOpaque(receiver, as: URL.ParseStrategy.self, typeName: "URL.ParseStrategy")
        return boxOpaque(recv.fragment(), typeName: "URL.ParseStrategy")
    },
    "func URL.ParseStrategy.parse()": .method { receiver, args in
        guard args.count == 1 else {
            throw RuntimeError.invalid("URL.ParseStrategy.parse: expected 1 argument(s), got \(args.count)")
        }
        let recv: URL.ParseStrategy = try unboxOpaque(receiver, as: URL.ParseStrategy.self, typeName: "URL.ParseStrategy")
        do {
            return boxOpaque(try recv.parse(try unboxString(args[0])), typeName: "URL")
        } catch {
            throw UserThrowSignal(value: .opaque(typeName: "Error", value: error))
        }
    },
    "init URL.ParseStrategy()": .`init` { args in
        guard args.count == 0 else {
            throw RuntimeError.invalid("init URL.ParseStrategy(): expected 0 argument(s), got \(args.count)")
        }
        return boxOpaque(URL.ParseStrategy(), typeName: "URL.ParseStrategy")
    },
    ]
}
#else
extension FoundationBridges {
    nonisolated(unsafe) static let uRLParseStrategy: [String: Bridge] = [:]
}
#endif
