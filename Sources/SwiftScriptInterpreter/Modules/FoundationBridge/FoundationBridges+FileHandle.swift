// AUTO-GENERATED by BridgeGeneratorTool. Do not edit by hand.
// Regenerate with: bash Tools/regen-foundation-bridge.sh
import Foundation
    import ShellKit
#if canImport(FoundationNetworking)
import FoundationNetworking
#endif

extension FoundationBridges {
    nonisolated(unsafe) static let fileHandle: [String: Bridge] = {
        var d: [String: Bridge] = [
    "func FileHandle.readToEnd()": .method { receiver, args in
        guard args.count == 0 else {
            throw RuntimeError.invalid("FileHandle.readToEnd: expected 0 argument(s), got \(args.count)")
        }
        let recv: FileHandle = try unboxOpaque(receiver, as: FileHandle.self, typeName: "FileHandle")
        do {
            if let _v = try recv.readToEnd() {
            return .optional(boxOpaque(_v, typeName: "Data"))
        }
        return .optional(nil)
        } catch {
            throw UserThrowSignal(value: .opaque(typeName: "Error", value: error))
        }
    },
    "func FileHandle.offset()": .method { receiver, args in
        guard args.count == 0 else {
            throw RuntimeError.invalid("FileHandle.offset: expected 0 argument(s), got \(args.count)")
        }
        let recv: FileHandle = try unboxOpaque(receiver, as: FileHandle.self, typeName: "FileHandle")
        do {
            return try boxUnsignedAsInt(try recv.offset())
        } catch {
            throw UserThrowSignal(value: .opaque(typeName: "Error", value: error))
        }
    },
    "func FileHandle.seekToEnd()": .method { receiver, args in
        guard args.count == 0 else {
            throw RuntimeError.invalid("FileHandle.seekToEnd: expected 0 argument(s), got \(args.count)")
        }
        let recv: FileHandle = try unboxOpaque(receiver, as: FileHandle.self, typeName: "FileHandle")
        do {
            return try boxUnsignedAsInt(try recv.seekToEnd())
        } catch {
            throw UserThrowSignal(value: .opaque(typeName: "Error", value: error))
        }
    },
    "var FileHandle.availableData: Data": .computed { receiver in
        let recv: FileHandle = try unboxOpaque(receiver, as: FileHandle.self, typeName: "FileHandle")
        return boxOpaque(recv.availableData, typeName: "Data")
    },
    "func FileHandle.synchronize()": .method { receiver, args in
        guard args.count == 0 else {
            throw RuntimeError.invalid("FileHandle.synchronize: expected 0 argument(s), got \(args.count)")
        }
        let recv: FileHandle = try unboxOpaque(receiver, as: FileHandle.self, typeName: "FileHandle")
        do {
            try recv.synchronize()
            return .void
        } catch {
            throw UserThrowSignal(value: .opaque(typeName: "Error", value: error))
        }
    },
    "func FileHandle.close()": .method { receiver, args in
        guard args.count == 0 else {
            throw RuntimeError.invalid("FileHandle.close: expected 0 argument(s), got \(args.count)")
        }
        let recv: FileHandle = try unboxOpaque(receiver, as: FileHandle.self, typeName: "FileHandle")
        do {
            try recv.close()
            return .void
        } catch {
            throw UserThrowSignal(value: .opaque(typeName: "Error", value: error))
        }
    },
    "static let FileHandle.standardInput": .staticValue(boxOpaque(FileHandle.standardInput, typeName: "FileHandle")),
    "static let FileHandle.standardOutput": .staticValue(boxOpaque(FileHandle.standardOutput, typeName: "FileHandle")),
    "static let FileHandle.standardError": .staticValue(boxOpaque(FileHandle.standardError, typeName: "FileHandle")),
    "static let FileHandle.nullDevice": .staticValue(boxOpaque(FileHandle.nullDevice, typeName: "FileHandle")),
    "func FileHandle.readInBackgroundAndNotify()": .method { receiver, args in
        guard args.count == 0 else {
            throw RuntimeError.invalid("FileHandle.readInBackgroundAndNotify: expected 0 argument(s), got \(args.count)")
        }
        let recv: FileHandle = try unboxOpaque(receiver, as: FileHandle.self, typeName: "FileHandle")
        recv.readInBackgroundAndNotify()
            return .void
    },
    "func FileHandle.readToEndOfFileInBackgroundAndNotify()": .method { receiver, args in
        guard args.count == 0 else {
            throw RuntimeError.invalid("FileHandle.readToEndOfFileInBackgroundAndNotify: expected 0 argument(s), got \(args.count)")
        }
        let recv: FileHandle = try unboxOpaque(receiver, as: FileHandle.self, typeName: "FileHandle")
        recv.readToEndOfFileInBackgroundAndNotify()
            return .void
    },
    "func FileHandle.waitForDataInBackgroundAndNotify()": .method { receiver, args in
        guard args.count == 0 else {
            throw RuntimeError.invalid("FileHandle.waitForDataInBackgroundAndNotify: expected 0 argument(s), got \(args.count)")
        }
        let recv: FileHandle = try unboxOpaque(receiver, as: FileHandle.self, typeName: "FileHandle")
        recv.waitForDataInBackgroundAndNotify()
            return .void
    },
    "func FileHandle.readDataToEndOfFile()": .method { receiver, args in
        guard args.count == 0 else {
            throw RuntimeError.invalid("FileHandle.readDataToEndOfFile: expected 0 argument(s), got \(args.count)")
        }
        let recv: FileHandle = try unboxOpaque(receiver, as: FileHandle.self, typeName: "FileHandle")
        return boxOpaque(recv.readDataToEndOfFile(), typeName: "Data")
    },
    "var FileHandle.offsetInFile: UInt64": .computed { receiver in
        let recv: FileHandle = try unboxOpaque(receiver, as: FileHandle.self, typeName: "FileHandle")
        return try boxUnsignedAsInt(recv.offsetInFile)
    },
    "func FileHandle.seekToEndOfFile()": .method { receiver, args in
        guard args.count == 0 else {
            throw RuntimeError.invalid("FileHandle.seekToEndOfFile: expected 0 argument(s), got \(args.count)")
        }
        let recv: FileHandle = try unboxOpaque(receiver, as: FileHandle.self, typeName: "FileHandle")
        return try boxUnsignedAsInt(recv.seekToEndOfFile())
    },
    "func FileHandle.synchronizeFile()": .method { receiver, args in
        guard args.count == 0 else {
            throw RuntimeError.invalid("FileHandle.synchronizeFile: expected 0 argument(s), got \(args.count)")
        }
        let recv: FileHandle = try unboxOpaque(receiver, as: FileHandle.self, typeName: "FileHandle")
        recv.synchronizeFile()
            return .void
    },
    "func FileHandle.closeFile()": .method { receiver, args in
        guard args.count == 0 else {
            throw RuntimeError.invalid("FileHandle.closeFile: expected 0 argument(s), got \(args.count)")
        }
        let recv: FileHandle = try unboxOpaque(receiver, as: FileHandle.self, typeName: "FileHandle")
        recv.closeFile()
            return .void
    },
    "static let FileHandle.readCompletionNotification": .staticValue(boxOpaque(FileHandle.readCompletionNotification, typeName: "NSNotification.Name")),
    "func FileHandle.read(upToCount:)": .method { receiver, args in
        guard args.count == 1 else {
            throw RuntimeError.invalid("FileHandle.read: expected 1 argument(s), got \(args.count)")
        }
        let recv: FileHandle = try unboxOpaque(receiver, as: FileHandle.self, typeName: "FileHandle")
        do {
            if let _v = try recv.read(upToCount: try unboxInt(args[0])) {
            return .optional(boxOpaque(_v, typeName: "Data"))
        }
        return .optional(nil)
        } catch {
            throw UserThrowSignal(value: .opaque(typeName: "Error", value: error))
        }
    },
    "func FileHandle.read()": .method { receiver, args in
        guard args.count == 1 else {
            throw RuntimeError.invalid("FileHandle.read: expected 1 argument(s), got \(args.count)")
        }
        let recv: FileHandle = try unboxOpaque(receiver, as: FileHandle.self, typeName: "FileHandle")
        do {
            if let _v = try recv.read(upToCount: try unboxInt(args[0])) {
            return .optional(boxOpaque(_v, typeName: "Data"))
        }
        return .optional(nil)
        } catch {
            throw UserThrowSignal(value: .opaque(typeName: "Error", value: error))
        }
    },
    "func FileHandle.seek(toOffset:)": .method { receiver, args in
        guard args.count == 1 else {
            throw RuntimeError.invalid("FileHandle.seek: expected 1 argument(s), got \(args.count)")
        }
        let recv: FileHandle = try unboxOpaque(receiver, as: FileHandle.self, typeName: "FileHandle")
        do {
            try recv.seek(toOffset: try toUInt64(args[0]))
            return .void
        } catch {
            throw UserThrowSignal(value: .opaque(typeName: "Error", value: error))
        }
    },
    "func FileHandle.seek()": .method { receiver, args in
        guard args.count == 1 else {
            throw RuntimeError.invalid("FileHandle.seek: expected 1 argument(s), got \(args.count)")
        }
        let recv: FileHandle = try unboxOpaque(receiver, as: FileHandle.self, typeName: "FileHandle")
        do {
            try recv.seek(toOffset: try toUInt64(args[0]))
            return .void
        } catch {
            throw UserThrowSignal(value: .opaque(typeName: "Error", value: error))
        }
    },
    "func FileHandle.truncate(atOffset:)": .method { receiver, args in
        guard args.count == 1 else {
            throw RuntimeError.invalid("FileHandle.truncate: expected 1 argument(s), got \(args.count)")
        }
        let recv: FileHandle = try unboxOpaque(receiver, as: FileHandle.self, typeName: "FileHandle")
        do {
            try recv.truncate(atOffset: try toUInt64(args[0]))
            return .void
        } catch {
            throw UserThrowSignal(value: .opaque(typeName: "Error", value: error))
        }
    },
    "func FileHandle.truncate()": .method { receiver, args in
        guard args.count == 1 else {
            throw RuntimeError.invalid("FileHandle.truncate: expected 1 argument(s), got \(args.count)")
        }
        let recv: FileHandle = try unboxOpaque(receiver, as: FileHandle.self, typeName: "FileHandle")
        do {
            try recv.truncate(atOffset: try toUInt64(args[0]))
            return .void
        } catch {
            throw UserThrowSignal(value: .opaque(typeName: "Error", value: error))
        }
    },
    "init FileHandle(forReadingAtPath:)": .`init` { args in
        guard args.count == 1 else {
            throw RuntimeError.invalid("init FileHandle(forReadingAtPath:): expected 1 argument(s), got \(args.count)")
        }
        var arg0 = try unboxString(args[0])
        do {
            arg0 = try await authorizePath(arg0, for: .read)
        } catch {
            throw UserThrowSignal(value: .opaque(typeName: "Error", value: error))
        }
        if let _v = await FileHandle(forReadingAtPath: arg0) {
            return .optional(boxOpaque(_v, typeName: "FileHandle"))
        }
        return .optional(nil)
    },
    "init FileHandle(forWritingAtPath:)": .`init` { args in
        guard args.count == 1 else {
            throw RuntimeError.invalid("init FileHandle(forWritingAtPath:): expected 1 argument(s), got \(args.count)")
        }
        var arg0 = try unboxString(args[0])
        do {
            arg0 = try await authorizePath(arg0, for: .write)
        } catch {
            throw UserThrowSignal(value: .opaque(typeName: "Error", value: error))
        }
        if let _v = await FileHandle(forWritingAtPath: arg0) {
            return .optional(boxOpaque(_v, typeName: "FileHandle"))
        }
        return .optional(nil)
    },
    "init FileHandle(forUpdatingAtPath:)": .`init` { args in
        guard args.count == 1 else {
            throw RuntimeError.invalid("init FileHandle(forUpdatingAtPath:): expected 1 argument(s), got \(args.count)")
        }
        var arg0 = try unboxString(args[0])
        do {
            arg0 = try await authorizePath(arg0, for: .write)
        } catch {
            throw UserThrowSignal(value: .opaque(typeName: "Error", value: error))
        }
        if let _v = await FileHandle(forUpdatingAtPath: arg0) {
            return .optional(boxOpaque(_v, typeName: "FileHandle"))
        }
        return .optional(nil)
    },
    "init FileHandle(forReadingFrom:)": .`init` { args in
        guard args.count == 1 else {
            throw RuntimeError.invalid("init FileHandle(forReadingFrom:): expected 1 argument(s), got \(args.count)")
        }
        var arg0 = try unboxOpaque(args[0], as: URL.self, typeName: "URL")
        do {
            arg0 = try await authorizePath(arg0, for: .read)
        } catch {
            throw UserThrowSignal(value: .opaque(typeName: "Error", value: error))
        }
        do {
            return boxOpaque(try await FileHandle(forReadingFrom: arg0), typeName: "FileHandle")
        } catch {
            throw UserThrowSignal(value: .opaque(typeName: "Error", value: error))
        }
    },
    "init FileHandle(forWritingTo:)": .`init` { args in
        guard args.count == 1 else {
            throw RuntimeError.invalid("init FileHandle(forWritingTo:): expected 1 argument(s), got \(args.count)")
        }
        var arg0 = try unboxOpaque(args[0], as: URL.self, typeName: "URL")
        do {
            arg0 = try await authorizePath(arg0, for: .write)
        } catch {
            throw UserThrowSignal(value: .opaque(typeName: "Error", value: error))
        }
        do {
            return boxOpaque(try await FileHandle(forWritingTo: arg0), typeName: "FileHandle")
        } catch {
            throw UserThrowSignal(value: .opaque(typeName: "Error", value: error))
        }
    },
    "init FileHandle(forUpdating:)": .`init` { args in
        guard args.count == 1 else {
            throw RuntimeError.invalid("init FileHandle(forUpdating:): expected 1 argument(s), got \(args.count)")
        }
        var arg0 = try unboxOpaque(args[0], as: URL.self, typeName: "URL")
        do {
            arg0 = try await authorizePath(arg0, for: .write)
        } catch {
            throw UserThrowSignal(value: .opaque(typeName: "Error", value: error))
        }
        do {
            return boxOpaque(try await FileHandle(forUpdating: arg0), typeName: "FileHandle")
        } catch {
            throw UserThrowSignal(value: .opaque(typeName: "Error", value: error))
        }
    },
    "func FileHandle.readInBackgroundAndNotify(forModes:)": .method { receiver, args in
        guard args.count == 1 else {
            throw RuntimeError.invalid("FileHandle.readInBackgroundAndNotify: expected 1 argument(s), got \(args.count)")
        }
        let recv: FileHandle = try unboxOpaque(receiver, as: FileHandle.self, typeName: "FileHandle")
        recv.readInBackgroundAndNotify(forModes: try unboxOptionalValue(args[0]).map { try unboxArray($0).map { try unboxOpaque($0, as: RunLoop.Mode.self, typeName: "RunLoop.Mode") } })
            return .void
    },
    "func FileHandle.readToEndOfFileInBackgroundAndNotify(forModes:)": .method { receiver, args in
        guard args.count == 1 else {
            throw RuntimeError.invalid("FileHandle.readToEndOfFileInBackgroundAndNotify: expected 1 argument(s), got \(args.count)")
        }
        let recv: FileHandle = try unboxOpaque(receiver, as: FileHandle.self, typeName: "FileHandle")
        recv.readToEndOfFileInBackgroundAndNotify(forModes: try unboxOptionalValue(args[0]).map { try unboxArray($0).map { try unboxOpaque($0, as: RunLoop.Mode.self, typeName: "RunLoop.Mode") } })
            return .void
    },
    "func FileHandle.waitForDataInBackgroundAndNotify(forModes:)": .method { receiver, args in
        guard args.count == 1 else {
            throw RuntimeError.invalid("FileHandle.waitForDataInBackgroundAndNotify: expected 1 argument(s), got \(args.count)")
        }
        let recv: FileHandle = try unboxOpaque(receiver, as: FileHandle.self, typeName: "FileHandle")
        recv.waitForDataInBackgroundAndNotify(forModes: try unboxOptionalValue(args[0]).map { try unboxArray($0).map { try unboxOpaque($0, as: RunLoop.Mode.self, typeName: "RunLoop.Mode") } })
            return .void
    },
    "init FileHandle(fileDescriptor:)": .`init` { args in
        guard args.count == 1 else {
            throw RuntimeError.invalid("init FileHandle(fileDescriptor:): expected 1 argument(s), got \(args.count)")
        }
        return boxOpaque(FileHandle(fileDescriptor: try toInt32(args[0])), typeName: "FileHandle")
    },
    "func FileHandle.readData(ofLength:)": .method { receiver, args in
        guard args.count == 1 else {
            throw RuntimeError.invalid("FileHandle.readData: expected 1 argument(s), got \(args.count)")
        }
        let recv: FileHandle = try unboxOpaque(receiver, as: FileHandle.self, typeName: "FileHandle")
        return boxOpaque(recv.readData(ofLength: try unboxInt(args[0])), typeName: "Data")
    },
    "func FileHandle.readData()": .method { receiver, args in
        guard args.count == 1 else {
            throw RuntimeError.invalid("FileHandle.readData: expected 1 argument(s), got \(args.count)")
        }
        let recv: FileHandle = try unboxOpaque(receiver, as: FileHandle.self, typeName: "FileHandle")
        return boxOpaque(recv.readData(ofLength: try unboxInt(args[0])), typeName: "Data")
    },
    "func FileHandle.write(_:)": .method { receiver, args in
        guard args.count == 1 else {
            throw RuntimeError.invalid("FileHandle.write: expected 1 argument(s), got \(args.count)")
        }
        let recv: FileHandle = try unboxOpaque(receiver, as: FileHandle.self, typeName: "FileHandle")
        recv.write(try unboxOpaque(args[0], as: Data.self, typeName: "Data"))
            return .void
    },
    "func FileHandle.write()": .method { receiver, args in
        guard args.count == 1 else {
            throw RuntimeError.invalid("FileHandle.write: expected 1 argument(s), got \(args.count)")
        }
        let recv: FileHandle = try unboxOpaque(receiver, as: FileHandle.self, typeName: "FileHandle")
        recv.write(try unboxOpaque(args[0], as: Data.self, typeName: "Data"))
            return .void
    },
    "func FileHandle.seek(toFileOffset:)": .method { receiver, args in
        guard args.count == 1 else {
            throw RuntimeError.invalid("FileHandle.seek: expected 1 argument(s), got \(args.count)")
        }
        let recv: FileHandle = try unboxOpaque(receiver, as: FileHandle.self, typeName: "FileHandle")
        recv.seek(toFileOffset: try toUInt64(args[0]))
            return .void
    },
    "func FileHandle.truncateFile(atOffset:)": .method { receiver, args in
        guard args.count == 1 else {
            throw RuntimeError.invalid("FileHandle.truncateFile: expected 1 argument(s), got \(args.count)")
        }
        let recv: FileHandle = try unboxOpaque(receiver, as: FileHandle.self, typeName: "FileHandle")
        recv.truncateFile(atOffset: try toUInt64(args[0]))
            return .void
    },
    "func FileHandle.truncateFile()": .method { receiver, args in
        guard args.count == 1 else {
            throw RuntimeError.invalid("FileHandle.truncateFile: expected 1 argument(s), got \(args.count)")
        }
        let recv: FileHandle = try unboxOpaque(receiver, as: FileHandle.self, typeName: "FileHandle")
        recv.truncateFile(atOffset: try toUInt64(args[0]))
            return .void
    },
    "init FileHandle(fileDescriptor:closeOnDealloc:)": .`init` { args in
        guard args.count == 2 else {
            throw RuntimeError.invalid("init FileHandle(fileDescriptor:closeOnDealloc:): expected 2 argument(s), got \(args.count)")
        }
        return boxOpaque(FileHandle(fileDescriptor: try toInt32(args[0]), closeOnDealloc: try unboxBool(args[1])), typeName: "FileHandle")
    },
        ]
        #if canImport(Darwin)
    d["func FileHandle.acceptConnectionInBackgroundAndNotify()"] = .method { receiver, args in
        guard args.count == 0 else {
            throw RuntimeError.invalid("FileHandle.acceptConnectionInBackgroundAndNotify: expected 0 argument(s), got \(args.count)")
        }
        let recv: FileHandle = try unboxOpaque(receiver, as: FileHandle.self, typeName: "FileHandle")
        recv.acceptConnectionInBackgroundAndNotify()
            return .void
    }
    d["func FileHandle.acceptConnectionInBackgroundAndNotify(forModes:)"] = .method { receiver, args in
        guard args.count == 1 else {
            throw RuntimeError.invalid("FileHandle.acceptConnectionInBackgroundAndNotify: expected 1 argument(s), got \(args.count)")
        }
        let recv: FileHandle = try unboxOpaque(receiver, as: FileHandle.self, typeName: "FileHandle")
        recv.acceptConnectionInBackgroundAndNotify(forModes: try unboxOptionalValue(args[0]).map { try unboxArray($0).map { try unboxOpaque($0, as: RunLoop.Mode.self, typeName: "RunLoop.Mode") } })
            return .void
    }
        #endif
        return d
    }()
}
