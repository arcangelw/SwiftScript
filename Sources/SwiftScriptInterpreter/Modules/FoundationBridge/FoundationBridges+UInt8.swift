// AUTO-GENERATED by BridgeGeneratorTool. Do not edit by hand.
// Regenerate with: bash Tools/regen-foundation-bridge.sh
import Foundation
import ShellKit
#if canImport(FoundationNetworking)
import FoundationNetworking
#endif

#if canImport(Darwin)
extension FoundationBridges {
    nonisolated(unsafe) static let uInt8: [String: Bridge] = [
    "init UInt8()": .`init` { args in
        guard args.count == 0 else {
            throw RuntimeError.invalid("init UInt8(): expected 0 argument(s), got \(args.count)")
        }
        return .int(Int(UInt8()))
    },
    "init UInt8(_:)": .`init` { args in
        guard args.count == 1 else {
            throw RuntimeError.invalid("init UInt8(_:): expected 1 argument(s), got \(args.count)")
        }
        if let _v = UInt8(try unboxString(args[0])) {
            return .optional(.int(Int(_v)))
        }
        return .optional(nil)
    },
    "init UInt8(integerLiteral:)": .`init` { args in
        guard args.count == 1 else {
            throw RuntimeError.invalid("init UInt8(integerLiteral:): expected 1 argument(s), got \(args.count)")
        }
        return .int(Int(UInt8(integerLiteral: try toUInt8(args[0]))))
    },
    "init UInt8(littleEndian:)": .`init` { args in
        guard args.count == 1 else {
            throw RuntimeError.invalid("init UInt8(littleEndian:): expected 1 argument(s), got \(args.count)")
        }
        return .int(Int(UInt8(littleEndian: try toUInt8(args[0]))))
    },
    "init UInt8(bigEndian:)": .`init` { args in
        guard args.count == 1 else {
            throw RuntimeError.invalid("init UInt8(bigEndian:): expected 1 argument(s), got \(args.count)")
        }
        return .int(Int(UInt8(bigEndian: try toUInt8(args[0]))))
    },
    "init UInt8(bitPattern:)": .`init` { args in
        guard args.count == 1 else {
            throw RuntimeError.invalid("init UInt8(bitPattern:): expected 1 argument(s), got \(args.count)")
        }
        return .int(Int(UInt8(bitPattern: try toInt8(args[0]))))
    },
    "init UInt8(exactly:)": .`init` { args in
        guard args.count == 1 else {
            throw RuntimeError.invalid("init UInt8(exactly:): expected 1 argument(s), got \(args.count)")
        }
        if let _v = UInt8(exactly: try toFloat(args[0])) {
            return .optional(.int(Int(_v)))
        }
        return .optional(nil)
    },
    "static let UInt8.zero": .staticValue(.int(Int(UInt8.zero))),
    "var UInt8.bitWidth: Int": .computed { receiver in
        let recv: UInt8 = try toUInt8(receiver)
        return .int(recv.bitWidth)
    },
    "var UInt8.littleEndian: UInt8": .computed { receiver in
        let recv: UInt8 = try toUInt8(receiver)
        return .int(Int(recv.littleEndian))
    },
    "var UInt8.bigEndian: UInt8": .computed { receiver in
        let recv: UInt8 = try toUInt8(receiver)
        return .int(Int(recv.bigEndian))
    },
    "var UInt8.description: String": .computed { receiver in
        let recv: UInt8 = try toUInt8(receiver)
        return .string(recv.description)
    },
    "var UInt8.magnitude: UInt8": .computed { receiver in
        let recv: UInt8 = try toUInt8(receiver)
        return .int(Int(recv.magnitude))
    },
    "static let UInt8.isSigned": .staticValue(.bool(UInt8.isSigned)),
    "static let UInt8.max": .staticValue(.int(Int(UInt8.max))),
    "static let UInt8.min": .staticValue(.int(Int(UInt8.min))),
    "func UInt8.formatted()": .method { receiver, args in
        guard args.count == 0 else {
            throw RuntimeError.invalid("UInt8.formatted: expected 0 argument(s), got \(args.count)")
        }
        let recv: UInt8 = try toUInt8(receiver)
        return .string(recv.formatted())
    },
    "func UInt8.quotientAndRemainder(dividingBy:)": .method { receiver, args in
        guard args.count == 1 else {
            throw RuntimeError.invalid("UInt8.quotientAndRemainder: expected 1 argument(s), got \(args.count)")
        }
        let recv: UInt8 = try toUInt8(receiver)
        let _t = recv.quotientAndRemainder(dividingBy: try toUInt8(args[0]))
        return .tuple([.int(Int(_t.0)), .int(Int(_t.1))])
    },
    "func UInt8.quotientAndRemainder()": .method { receiver, args in
        guard args.count == 1 else {
            throw RuntimeError.invalid("UInt8.quotientAndRemainder: expected 1 argument(s), got \(args.count)")
        }
        let recv: UInt8 = try toUInt8(receiver)
        let _t = recv.quotientAndRemainder(dividingBy: try toUInt8(args[0]))
        return .tuple([.int(Int(_t.0)), .int(Int(_t.1))])
    },
    "func UInt8.isMultiple(of:)": .method { receiver, args in
        guard args.count == 1 else {
            throw RuntimeError.invalid("UInt8.isMultiple: expected 1 argument(s), got \(args.count)")
        }
        let recv: UInt8 = try toUInt8(receiver)
        return .bool(recv.isMultiple(of: try toUInt8(args[0])))
    },
    "func UInt8.isMultiple()": .method { receiver, args in
        guard args.count == 1 else {
            throw RuntimeError.invalid("UInt8.isMultiple: expected 1 argument(s), got \(args.count)")
        }
        let recv: UInt8 = try toUInt8(receiver)
        return .bool(recv.isMultiple(of: try toUInt8(args[0])))
    },
    "func UInt8.distance(to:)": .method { receiver, args in
        guard args.count == 1 else {
            throw RuntimeError.invalid("UInt8.distance: expected 1 argument(s), got \(args.count)")
        }
        let recv: UInt8 = try toUInt8(receiver)
        return .int(recv.distance(to: try toUInt8(args[0])))
    },
    "func UInt8.distance()": .method { receiver, args in
        guard args.count == 1 else {
            throw RuntimeError.invalid("UInt8.distance: expected 1 argument(s), got \(args.count)")
        }
        let recv: UInt8 = try toUInt8(receiver)
        return .int(recv.distance(to: try toUInt8(args[0])))
    },
    "func UInt8.advanced(by:)": .method { receiver, args in
        guard args.count == 1 else {
            throw RuntimeError.invalid("UInt8.advanced: expected 1 argument(s), got \(args.count)")
        }
        let recv: UInt8 = try toUInt8(receiver)
        return .int(Int(recv.advanced(by: try unboxInt(args[0]))))
    },
    "func UInt8.advanced()": .method { receiver, args in
        guard args.count == 1 else {
            throw RuntimeError.invalid("UInt8.advanced: expected 1 argument(s), got \(args.count)")
        }
        let recv: UInt8 = try toUInt8(receiver)
        return .int(Int(recv.advanced(by: try unboxInt(args[0]))))
    },
    "static let UInt8.bitWidth": .staticValue(.int(UInt8.bitWidth)),
    "var UInt8.leadingZeroBitCount: Int": .computed { receiver in
        let recv: UInt8 = try toUInt8(receiver)
        return .int(recv.leadingZeroBitCount)
    },
    "var UInt8.trailingZeroBitCount: Int": .computed { receiver in
        let recv: UInt8 = try toUInt8(receiver)
        return .int(recv.trailingZeroBitCount)
    },
    "var UInt8.nonzeroBitCount: Int": .computed { receiver in
        let recv: UInt8 = try toUInt8(receiver)
        return .int(recv.nonzeroBitCount)
    },
    "var UInt8.byteSwapped: UInt8": .computed { receiver in
        let recv: UInt8 = try toUInt8(receiver)
        return .int(Int(recv.byteSwapped))
    },
    "func UInt8.signum()": .method { receiver, args in
        guard args.count == 0 else {
            throw RuntimeError.invalid("UInt8.signum: expected 0 argument(s), got \(args.count)")
        }
        let recv: UInt8 = try toUInt8(receiver)
        return .int(Int(recv.signum()))
    },
    "var UInt8.hashValue: Int": .computed { receiver in
        let recv: UInt8 = try toUInt8(receiver)
        return .int(recv.hashValue)
    },
    "func UInt8.addingReportingOverflow(_:)": .method { receiver, args in
        guard args.count == 1 else {
            throw RuntimeError.invalid("UInt8.addingReportingOverflow: expected 1 argument(s), got \(args.count)")
        }
        let recv: UInt8 = try toUInt8(receiver)
        let _t = recv.addingReportingOverflow(try toUInt8(args[0]))
        return .tuple([.int(Int(_t.0)), .bool(_t.1)])
    },
    "func UInt8.addingReportingOverflow()": .method { receiver, args in
        guard args.count == 1 else {
            throw RuntimeError.invalid("UInt8.addingReportingOverflow: expected 1 argument(s), got \(args.count)")
        }
        let recv: UInt8 = try toUInt8(receiver)
        let _t = recv.addingReportingOverflow(try toUInt8(args[0]))
        return .tuple([.int(Int(_t.0)), .bool(_t.1)])
    },
    "func UInt8.subtractingReportingOverflow(_:)": .method { receiver, args in
        guard args.count == 1 else {
            throw RuntimeError.invalid("UInt8.subtractingReportingOverflow: expected 1 argument(s), got \(args.count)")
        }
        let recv: UInt8 = try toUInt8(receiver)
        let _t = recv.subtractingReportingOverflow(try toUInt8(args[0]))
        return .tuple([.int(Int(_t.0)), .bool(_t.1)])
    },
    "func UInt8.subtractingReportingOverflow()": .method { receiver, args in
        guard args.count == 1 else {
            throw RuntimeError.invalid("UInt8.subtractingReportingOverflow: expected 1 argument(s), got \(args.count)")
        }
        let recv: UInt8 = try toUInt8(receiver)
        let _t = recv.subtractingReportingOverflow(try toUInt8(args[0]))
        return .tuple([.int(Int(_t.0)), .bool(_t.1)])
    },
    "func UInt8.multipliedReportingOverflow(by:)": .method { receiver, args in
        guard args.count == 1 else {
            throw RuntimeError.invalid("UInt8.multipliedReportingOverflow: expected 1 argument(s), got \(args.count)")
        }
        let recv: UInt8 = try toUInt8(receiver)
        let _t = recv.multipliedReportingOverflow(by: try toUInt8(args[0]))
        return .tuple([.int(Int(_t.0)), .bool(_t.1)])
    },
    "func UInt8.multipliedReportingOverflow()": .method { receiver, args in
        guard args.count == 1 else {
            throw RuntimeError.invalid("UInt8.multipliedReportingOverflow: expected 1 argument(s), got \(args.count)")
        }
        let recv: UInt8 = try toUInt8(receiver)
        let _t = recv.multipliedReportingOverflow(by: try toUInt8(args[0]))
        return .tuple([.int(Int(_t.0)), .bool(_t.1)])
    },
    "func UInt8.dividedReportingOverflow(by:)": .method { receiver, args in
        guard args.count == 1 else {
            throw RuntimeError.invalid("UInt8.dividedReportingOverflow: expected 1 argument(s), got \(args.count)")
        }
        let recv: UInt8 = try toUInt8(receiver)
        let _t = recv.dividedReportingOverflow(by: try toUInt8(args[0]))
        return .tuple([.int(Int(_t.0)), .bool(_t.1)])
    },
    "func UInt8.dividedReportingOverflow()": .method { receiver, args in
        guard args.count == 1 else {
            throw RuntimeError.invalid("UInt8.dividedReportingOverflow: expected 1 argument(s), got \(args.count)")
        }
        let recv: UInt8 = try toUInt8(receiver)
        let _t = recv.dividedReportingOverflow(by: try toUInt8(args[0]))
        return .tuple([.int(Int(_t.0)), .bool(_t.1)])
    },
    "func UInt8.remainderReportingOverflow(dividingBy:)": .method { receiver, args in
        guard args.count == 1 else {
            throw RuntimeError.invalid("UInt8.remainderReportingOverflow: expected 1 argument(s), got \(args.count)")
        }
        let recv: UInt8 = try toUInt8(receiver)
        let _t = recv.remainderReportingOverflow(dividingBy: try toUInt8(args[0]))
        return .tuple([.int(Int(_t.0)), .bool(_t.1)])
    },
    "func UInt8.remainderReportingOverflow()": .method { receiver, args in
        guard args.count == 1 else {
            throw RuntimeError.invalid("UInt8.remainderReportingOverflow: expected 1 argument(s), got \(args.count)")
        }
        let recv: UInt8 = try toUInt8(receiver)
        let _t = recv.remainderReportingOverflow(dividingBy: try toUInt8(args[0]))
        return .tuple([.int(Int(_t.0)), .bool(_t.1)])
    },
    ]
}
#else
extension FoundationBridges {
    nonisolated(unsafe) static let uInt8: [String: Bridge] = [:]
}
#endif
