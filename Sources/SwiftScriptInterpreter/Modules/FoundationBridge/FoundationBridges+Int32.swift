// AUTO-GENERATED by BridgeGeneratorTool. Do not edit by hand.
// Regenerate with: bash Tools/regen-foundation-bridge.sh
import Foundation
import ShellKit
#if canImport(FoundationNetworking)
import FoundationNetworking
#endif

#if canImport(Darwin)
extension FoundationBridges {
    nonisolated(unsafe) static let int32: [String: Bridge] = [
    "init Int32()": .`init` { args in
        guard args.count == 0 else {
            throw RuntimeError.invalid("init Int32(): expected 0 argument(s), got \(args.count)")
        }
        return .int(Int(Int32()))
    },
    "init Int32(_:)": .`init` { args in
        guard args.count == 1 else {
            throw RuntimeError.invalid("init Int32(_:): expected 1 argument(s), got \(args.count)")
        }
        if let _v = Int32(try unboxString(args[0])) {
            return .optional(.int(Int(_v)))
        }
        return .optional(nil)
    },
    "init Int32(integerLiteral:)": .`init` { args in
        guard args.count == 1 else {
            throw RuntimeError.invalid("init Int32(integerLiteral:): expected 1 argument(s), got \(args.count)")
        }
        return .int(Int(Int32(integerLiteral: try toInt32(args[0]))))
    },
    "init Int32(littleEndian:)": .`init` { args in
        guard args.count == 1 else {
            throw RuntimeError.invalid("init Int32(littleEndian:): expected 1 argument(s), got \(args.count)")
        }
        return .int(Int(Int32(littleEndian: try toInt32(args[0]))))
    },
    "init Int32(bigEndian:)": .`init` { args in
        guard args.count == 1 else {
            throw RuntimeError.invalid("init Int32(bigEndian:): expected 1 argument(s), got \(args.count)")
        }
        return .int(Int(Int32(bigEndian: try toInt32(args[0]))))
    },
    "init Int32(bitPattern:)": .`init` { args in
        guard args.count == 1 else {
            throw RuntimeError.invalid("init Int32(bitPattern:): expected 1 argument(s), got \(args.count)")
        }
        return .int(Int(Int32(bitPattern: try toUInt32(args[0]))))
    },
    "init Int32(exactly:)": .`init` { args in
        guard args.count == 1 else {
            throw RuntimeError.invalid("init Int32(exactly:): expected 1 argument(s), got \(args.count)")
        }
        if let _v = Int32(exactly: try toFloat(args[0])) {
            return .optional(.int(Int(_v)))
        }
        return .optional(nil)
    },
    "static let Int32.zero": .staticValue(.int(Int(Int32.zero))),
    "var Int32.description: String": .computed { receiver in
        let recv: Int32 = try toInt32(receiver)
        return .string(recv.description)
    },
    "var Int32.bitWidth: Int": .computed { receiver in
        let recv: Int32 = try toInt32(receiver)
        return .int(recv.bitWidth)
    },
    "var Int32.littleEndian: Int32": .computed { receiver in
        let recv: Int32 = try toInt32(receiver)
        return .int(Int(recv.littleEndian))
    },
    "var Int32.bigEndian: Int32": .computed { receiver in
        let recv: Int32 = try toInt32(receiver)
        return .int(Int(recv.bigEndian))
    },
    "static let Int32.isSigned": .staticValue(.bool(Int32.isSigned)),
    "static let Int32.max": .staticValue(.int(Int(Int32.max))),
    "static let Int32.min": .staticValue(.int(Int(Int32.min))),
    "func Int32.formatted()": .method { receiver, args in
        guard args.count == 0 else {
            throw RuntimeError.invalid("Int32.formatted: expected 0 argument(s), got \(args.count)")
        }
        let recv: Int32 = try toInt32(receiver)
        return .string(recv.formatted())
    },
    "func Int32.quotientAndRemainder(dividingBy:)": .method { receiver, args in
        guard args.count == 1 else {
            throw RuntimeError.invalid("Int32.quotientAndRemainder: expected 1 argument(s), got \(args.count)")
        }
        let recv: Int32 = try toInt32(receiver)
        let _t = recv.quotientAndRemainder(dividingBy: try toInt32(args[0]))
        return .tuple([.int(Int(_t.0)), .int(Int(_t.1))])
    },
    "func Int32.quotientAndRemainder()": .method { receiver, args in
        guard args.count == 1 else {
            throw RuntimeError.invalid("Int32.quotientAndRemainder: expected 1 argument(s), got \(args.count)")
        }
        let recv: Int32 = try toInt32(receiver)
        let _t = recv.quotientAndRemainder(dividingBy: try toInt32(args[0]))
        return .tuple([.int(Int(_t.0)), .int(Int(_t.1))])
    },
    "func Int32.distance(to:)": .method { receiver, args in
        guard args.count == 1 else {
            throw RuntimeError.invalid("Int32.distance: expected 1 argument(s), got \(args.count)")
        }
        let recv: Int32 = try toInt32(receiver)
        return .int(recv.distance(to: try toInt32(args[0])))
    },
    "func Int32.distance()": .method { receiver, args in
        guard args.count == 1 else {
            throw RuntimeError.invalid("Int32.distance: expected 1 argument(s), got \(args.count)")
        }
        let recv: Int32 = try toInt32(receiver)
        return .int(recv.distance(to: try toInt32(args[0])))
    },
    "func Int32.advanced(by:)": .method { receiver, args in
        guard args.count == 1 else {
            throw RuntimeError.invalid("Int32.advanced: expected 1 argument(s), got \(args.count)")
        }
        let recv: Int32 = try toInt32(receiver)
        return .int(Int(recv.advanced(by: try unboxInt(args[0]))))
    },
    "func Int32.advanced()": .method { receiver, args in
        guard args.count == 1 else {
            throw RuntimeError.invalid("Int32.advanced: expected 1 argument(s), got \(args.count)")
        }
        let recv: Int32 = try toInt32(receiver)
        return .int(Int(recv.advanced(by: try unboxInt(args[0]))))
    },
    "func Int32.isMultiple(of:)": .method { receiver, args in
        guard args.count == 1 else {
            throw RuntimeError.invalid("Int32.isMultiple: expected 1 argument(s), got \(args.count)")
        }
        let recv: Int32 = try toInt32(receiver)
        return .bool(recv.isMultiple(of: try toInt32(args[0])))
    },
    "func Int32.isMultiple()": .method { receiver, args in
        guard args.count == 1 else {
            throw RuntimeError.invalid("Int32.isMultiple: expected 1 argument(s), got \(args.count)")
        }
        let recv: Int32 = try toInt32(receiver)
        return .bool(recv.isMultiple(of: try toInt32(args[0])))
    },
    "static let Int32.bitWidth": .staticValue(.int(Int32.bitWidth)),
    "var Int32.leadingZeroBitCount: Int": .computed { receiver in
        let recv: Int32 = try toInt32(receiver)
        return .int(recv.leadingZeroBitCount)
    },
    "var Int32.trailingZeroBitCount: Int": .computed { receiver in
        let recv: Int32 = try toInt32(receiver)
        return .int(recv.trailingZeroBitCount)
    },
    "var Int32.nonzeroBitCount: Int": .computed { receiver in
        let recv: Int32 = try toInt32(receiver)
        return .int(recv.nonzeroBitCount)
    },
    "var Int32.magnitude: UInt32": .computed { receiver in
        let recv: Int32 = try toInt32(receiver)
        return .int(Int(recv.magnitude))
    },
    "var Int32.byteSwapped: Int32": .computed { receiver in
        let recv: Int32 = try toInt32(receiver)
        return .int(Int(recv.byteSwapped))
    },
    "func Int32.signum()": .method { receiver, args in
        guard args.count == 0 else {
            throw RuntimeError.invalid("Int32.signum: expected 0 argument(s), got \(args.count)")
        }
        let recv: Int32 = try toInt32(receiver)
        return .int(Int(recv.signum()))
    },
    "var Int32.hashValue: Int": .computed { receiver in
        let recv: Int32 = try toInt32(receiver)
        return .int(recv.hashValue)
    },
    "func Int32.addingReportingOverflow(_:)": .method { receiver, args in
        guard args.count == 1 else {
            throw RuntimeError.invalid("Int32.addingReportingOverflow: expected 1 argument(s), got \(args.count)")
        }
        let recv: Int32 = try toInt32(receiver)
        let _t = recv.addingReportingOverflow(try toInt32(args[0]))
        return .tuple([.int(Int(_t.0)), .bool(_t.1)])
    },
    "func Int32.addingReportingOverflow()": .method { receiver, args in
        guard args.count == 1 else {
            throw RuntimeError.invalid("Int32.addingReportingOverflow: expected 1 argument(s), got \(args.count)")
        }
        let recv: Int32 = try toInt32(receiver)
        let _t = recv.addingReportingOverflow(try toInt32(args[0]))
        return .tuple([.int(Int(_t.0)), .bool(_t.1)])
    },
    "func Int32.subtractingReportingOverflow(_:)": .method { receiver, args in
        guard args.count == 1 else {
            throw RuntimeError.invalid("Int32.subtractingReportingOverflow: expected 1 argument(s), got \(args.count)")
        }
        let recv: Int32 = try toInt32(receiver)
        let _t = recv.subtractingReportingOverflow(try toInt32(args[0]))
        return .tuple([.int(Int(_t.0)), .bool(_t.1)])
    },
    "func Int32.subtractingReportingOverflow()": .method { receiver, args in
        guard args.count == 1 else {
            throw RuntimeError.invalid("Int32.subtractingReportingOverflow: expected 1 argument(s), got \(args.count)")
        }
        let recv: Int32 = try toInt32(receiver)
        let _t = recv.subtractingReportingOverflow(try toInt32(args[0]))
        return .tuple([.int(Int(_t.0)), .bool(_t.1)])
    },
    "func Int32.multipliedReportingOverflow(by:)": .method { receiver, args in
        guard args.count == 1 else {
            throw RuntimeError.invalid("Int32.multipliedReportingOverflow: expected 1 argument(s), got \(args.count)")
        }
        let recv: Int32 = try toInt32(receiver)
        let _t = recv.multipliedReportingOverflow(by: try toInt32(args[0]))
        return .tuple([.int(Int(_t.0)), .bool(_t.1)])
    },
    "func Int32.multipliedReportingOverflow()": .method { receiver, args in
        guard args.count == 1 else {
            throw RuntimeError.invalid("Int32.multipliedReportingOverflow: expected 1 argument(s), got \(args.count)")
        }
        let recv: Int32 = try toInt32(receiver)
        let _t = recv.multipliedReportingOverflow(by: try toInt32(args[0]))
        return .tuple([.int(Int(_t.0)), .bool(_t.1)])
    },
    "func Int32.dividedReportingOverflow(by:)": .method { receiver, args in
        guard args.count == 1 else {
            throw RuntimeError.invalid("Int32.dividedReportingOverflow: expected 1 argument(s), got \(args.count)")
        }
        let recv: Int32 = try toInt32(receiver)
        let _t = recv.dividedReportingOverflow(by: try toInt32(args[0]))
        return .tuple([.int(Int(_t.0)), .bool(_t.1)])
    },
    "func Int32.dividedReportingOverflow()": .method { receiver, args in
        guard args.count == 1 else {
            throw RuntimeError.invalid("Int32.dividedReportingOverflow: expected 1 argument(s), got \(args.count)")
        }
        let recv: Int32 = try toInt32(receiver)
        let _t = recv.dividedReportingOverflow(by: try toInt32(args[0]))
        return .tuple([.int(Int(_t.0)), .bool(_t.1)])
    },
    "func Int32.remainderReportingOverflow(dividingBy:)": .method { receiver, args in
        guard args.count == 1 else {
            throw RuntimeError.invalid("Int32.remainderReportingOverflow: expected 1 argument(s), got \(args.count)")
        }
        let recv: Int32 = try toInt32(receiver)
        let _t = recv.remainderReportingOverflow(dividingBy: try toInt32(args[0]))
        return .tuple([.int(Int(_t.0)), .bool(_t.1)])
    },
    "func Int32.remainderReportingOverflow()": .method { receiver, args in
        guard args.count == 1 else {
            throw RuntimeError.invalid("Int32.remainderReportingOverflow: expected 1 argument(s), got \(args.count)")
        }
        let recv: Int32 = try toInt32(receiver)
        let _t = recv.remainderReportingOverflow(dividingBy: try toInt32(args[0]))
        return .tuple([.int(Int(_t.0)), .bool(_t.1)])
    },
    ]
}
#else
extension FoundationBridges {
    nonisolated(unsafe) static let int32: [String: Bridge] = [:]
}
#endif
