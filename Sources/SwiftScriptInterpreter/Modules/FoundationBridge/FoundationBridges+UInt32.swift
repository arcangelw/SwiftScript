// AUTO-GENERATED by BridgeGeneratorTool. Do not edit by hand.
// Regenerate with: bash Tools/regen-foundation-bridge.sh
import Foundation
import ShellKit
#if canImport(FoundationNetworking)
import FoundationNetworking
#endif

#if canImport(Darwin)
extension FoundationBridges {
    nonisolated(unsafe) static let uInt32: [String: Bridge] = [
    "init UInt32()": .`init` { args in
        guard args.count == 0 else {
            throw RuntimeError.invalid("init UInt32(): expected 0 argument(s), got \(args.count)")
        }
        return .int(Int(UInt32()))
    },
    "init UInt32(_:)": .`init` { args in
        guard args.count == 1 else {
            throw RuntimeError.invalid("init UInt32(_:): expected 1 argument(s), got \(args.count)")
        }
        if let _v = UInt32(try unboxString(args[0])) {
            return .optional(.int(Int(_v)))
        }
        return .optional(nil)
    },
    "init UInt32(integerLiteral:)": .`init` { args in
        guard args.count == 1 else {
            throw RuntimeError.invalid("init UInt32(integerLiteral:): expected 1 argument(s), got \(args.count)")
        }
        return .int(Int(UInt32(integerLiteral: try toUInt32(args[0]))))
    },
    "init UInt32(littleEndian:)": .`init` { args in
        guard args.count == 1 else {
            throw RuntimeError.invalid("init UInt32(littleEndian:): expected 1 argument(s), got \(args.count)")
        }
        return .int(Int(UInt32(littleEndian: try toUInt32(args[0]))))
    },
    "init UInt32(bigEndian:)": .`init` { args in
        guard args.count == 1 else {
            throw RuntimeError.invalid("init UInt32(bigEndian:): expected 1 argument(s), got \(args.count)")
        }
        return .int(Int(UInt32(bigEndian: try toUInt32(args[0]))))
    },
    "init UInt32(bitPattern:)": .`init` { args in
        guard args.count == 1 else {
            throw RuntimeError.invalid("init UInt32(bitPattern:): expected 1 argument(s), got \(args.count)")
        }
        return .int(Int(UInt32(bitPattern: try toInt32(args[0]))))
    },
    "init UInt32(exactly:)": .`init` { args in
        guard args.count == 1 else {
            throw RuntimeError.invalid("init UInt32(exactly:): expected 1 argument(s), got \(args.count)")
        }
        if let _v = UInt32(exactly: try toFloat(args[0])) {
            return .optional(.int(Int(_v)))
        }
        return .optional(nil)
    },
    "static let UInt32.zero": .staticValue(.int(Int(UInt32.zero))),
    "var UInt32.bitWidth: Int": .computed { receiver in
        let recv: UInt32 = try toUInt32(receiver)
        return .int(recv.bitWidth)
    },
    "var UInt32.littleEndian: UInt32": .computed { receiver in
        let recv: UInt32 = try toUInt32(receiver)
        return .int(Int(recv.littleEndian))
    },
    "var UInt32.bigEndian: UInt32": .computed { receiver in
        let recv: UInt32 = try toUInt32(receiver)
        return .int(Int(recv.bigEndian))
    },
    "var UInt32.description: String": .computed { receiver in
        let recv: UInt32 = try toUInt32(receiver)
        return .string(recv.description)
    },
    "var UInt32.magnitude: UInt32": .computed { receiver in
        let recv: UInt32 = try toUInt32(receiver)
        return .int(Int(recv.magnitude))
    },
    "static let UInt32.isSigned": .staticValue(.bool(UInt32.isSigned)),
    "static let UInt32.max": .staticValue(.int(Int(UInt32.max))),
    "static let UInt32.min": .staticValue(.int(Int(UInt32.min))),
    "func UInt32.formatted()": .method { receiver, args in
        guard args.count == 0 else {
            throw RuntimeError.invalid("UInt32.formatted: expected 0 argument(s), got \(args.count)")
        }
        let recv: UInt32 = try toUInt32(receiver)
        return .string(recv.formatted())
    },
    "func UInt32.quotientAndRemainder(dividingBy:)": .method { receiver, args in
        guard args.count == 1 else {
            throw RuntimeError.invalid("UInt32.quotientAndRemainder: expected 1 argument(s), got \(args.count)")
        }
        let recv: UInt32 = try toUInt32(receiver)
        let _t = recv.quotientAndRemainder(dividingBy: try toUInt32(args[0]))
        return .tuple([.int(Int(_t.0)), .int(Int(_t.1))])
    },
    "func UInt32.quotientAndRemainder()": .method { receiver, args in
        guard args.count == 1 else {
            throw RuntimeError.invalid("UInt32.quotientAndRemainder: expected 1 argument(s), got \(args.count)")
        }
        let recv: UInt32 = try toUInt32(receiver)
        let _t = recv.quotientAndRemainder(dividingBy: try toUInt32(args[0]))
        return .tuple([.int(Int(_t.0)), .int(Int(_t.1))])
    },
    "func UInt32.isMultiple(of:)": .method { receiver, args in
        guard args.count == 1 else {
            throw RuntimeError.invalid("UInt32.isMultiple: expected 1 argument(s), got \(args.count)")
        }
        let recv: UInt32 = try toUInt32(receiver)
        return .bool(recv.isMultiple(of: try toUInt32(args[0])))
    },
    "func UInt32.isMultiple()": .method { receiver, args in
        guard args.count == 1 else {
            throw RuntimeError.invalid("UInt32.isMultiple: expected 1 argument(s), got \(args.count)")
        }
        let recv: UInt32 = try toUInt32(receiver)
        return .bool(recv.isMultiple(of: try toUInt32(args[0])))
    },
    "func UInt32.distance(to:)": .method { receiver, args in
        guard args.count == 1 else {
            throw RuntimeError.invalid("UInt32.distance: expected 1 argument(s), got \(args.count)")
        }
        let recv: UInt32 = try toUInt32(receiver)
        return .int(recv.distance(to: try toUInt32(args[0])))
    },
    "func UInt32.distance()": .method { receiver, args in
        guard args.count == 1 else {
            throw RuntimeError.invalid("UInt32.distance: expected 1 argument(s), got \(args.count)")
        }
        let recv: UInt32 = try toUInt32(receiver)
        return .int(recv.distance(to: try toUInt32(args[0])))
    },
    "func UInt32.advanced(by:)": .method { receiver, args in
        guard args.count == 1 else {
            throw RuntimeError.invalid("UInt32.advanced: expected 1 argument(s), got \(args.count)")
        }
        let recv: UInt32 = try toUInt32(receiver)
        return .int(Int(recv.advanced(by: try unboxInt(args[0]))))
    },
    "func UInt32.advanced()": .method { receiver, args in
        guard args.count == 1 else {
            throw RuntimeError.invalid("UInt32.advanced: expected 1 argument(s), got \(args.count)")
        }
        let recv: UInt32 = try toUInt32(receiver)
        return .int(Int(recv.advanced(by: try unboxInt(args[0]))))
    },
    "static let UInt32.bitWidth": .staticValue(.int(UInt32.bitWidth)),
    "var UInt32.leadingZeroBitCount: Int": .computed { receiver in
        let recv: UInt32 = try toUInt32(receiver)
        return .int(recv.leadingZeroBitCount)
    },
    "var UInt32.trailingZeroBitCount: Int": .computed { receiver in
        let recv: UInt32 = try toUInt32(receiver)
        return .int(recv.trailingZeroBitCount)
    },
    "var UInt32.nonzeroBitCount: Int": .computed { receiver in
        let recv: UInt32 = try toUInt32(receiver)
        return .int(recv.nonzeroBitCount)
    },
    "var UInt32.byteSwapped: UInt32": .computed { receiver in
        let recv: UInt32 = try toUInt32(receiver)
        return .int(Int(recv.byteSwapped))
    },
    "func UInt32.signum()": .method { receiver, args in
        guard args.count == 0 else {
            throw RuntimeError.invalid("UInt32.signum: expected 0 argument(s), got \(args.count)")
        }
        let recv: UInt32 = try toUInt32(receiver)
        return .int(Int(recv.signum()))
    },
    "var UInt32.hashValue: Int": .computed { receiver in
        let recv: UInt32 = try toUInt32(receiver)
        return .int(recv.hashValue)
    },
    "func UInt32.addingReportingOverflow(_:)": .method { receiver, args in
        guard args.count == 1 else {
            throw RuntimeError.invalid("UInt32.addingReportingOverflow: expected 1 argument(s), got \(args.count)")
        }
        let recv: UInt32 = try toUInt32(receiver)
        let _t = recv.addingReportingOverflow(try toUInt32(args[0]))
        return .tuple([.int(Int(_t.0)), .bool(_t.1)])
    },
    "func UInt32.addingReportingOverflow()": .method { receiver, args in
        guard args.count == 1 else {
            throw RuntimeError.invalid("UInt32.addingReportingOverflow: expected 1 argument(s), got \(args.count)")
        }
        let recv: UInt32 = try toUInt32(receiver)
        let _t = recv.addingReportingOverflow(try toUInt32(args[0]))
        return .tuple([.int(Int(_t.0)), .bool(_t.1)])
    },
    "func UInt32.subtractingReportingOverflow(_:)": .method { receiver, args in
        guard args.count == 1 else {
            throw RuntimeError.invalid("UInt32.subtractingReportingOverflow: expected 1 argument(s), got \(args.count)")
        }
        let recv: UInt32 = try toUInt32(receiver)
        let _t = recv.subtractingReportingOverflow(try toUInt32(args[0]))
        return .tuple([.int(Int(_t.0)), .bool(_t.1)])
    },
    "func UInt32.subtractingReportingOverflow()": .method { receiver, args in
        guard args.count == 1 else {
            throw RuntimeError.invalid("UInt32.subtractingReportingOverflow: expected 1 argument(s), got \(args.count)")
        }
        let recv: UInt32 = try toUInt32(receiver)
        let _t = recv.subtractingReportingOverflow(try toUInt32(args[0]))
        return .tuple([.int(Int(_t.0)), .bool(_t.1)])
    },
    "func UInt32.multipliedReportingOverflow(by:)": .method { receiver, args in
        guard args.count == 1 else {
            throw RuntimeError.invalid("UInt32.multipliedReportingOverflow: expected 1 argument(s), got \(args.count)")
        }
        let recv: UInt32 = try toUInt32(receiver)
        let _t = recv.multipliedReportingOverflow(by: try toUInt32(args[0]))
        return .tuple([.int(Int(_t.0)), .bool(_t.1)])
    },
    "func UInt32.multipliedReportingOverflow()": .method { receiver, args in
        guard args.count == 1 else {
            throw RuntimeError.invalid("UInt32.multipliedReportingOverflow: expected 1 argument(s), got \(args.count)")
        }
        let recv: UInt32 = try toUInt32(receiver)
        let _t = recv.multipliedReportingOverflow(by: try toUInt32(args[0]))
        return .tuple([.int(Int(_t.0)), .bool(_t.1)])
    },
    "func UInt32.dividedReportingOverflow(by:)": .method { receiver, args in
        guard args.count == 1 else {
            throw RuntimeError.invalid("UInt32.dividedReportingOverflow: expected 1 argument(s), got \(args.count)")
        }
        let recv: UInt32 = try toUInt32(receiver)
        let _t = recv.dividedReportingOverflow(by: try toUInt32(args[0]))
        return .tuple([.int(Int(_t.0)), .bool(_t.1)])
    },
    "func UInt32.dividedReportingOverflow()": .method { receiver, args in
        guard args.count == 1 else {
            throw RuntimeError.invalid("UInt32.dividedReportingOverflow: expected 1 argument(s), got \(args.count)")
        }
        let recv: UInt32 = try toUInt32(receiver)
        let _t = recv.dividedReportingOverflow(by: try toUInt32(args[0]))
        return .tuple([.int(Int(_t.0)), .bool(_t.1)])
    },
    "func UInt32.remainderReportingOverflow(dividingBy:)": .method { receiver, args in
        guard args.count == 1 else {
            throw RuntimeError.invalid("UInt32.remainderReportingOverflow: expected 1 argument(s), got \(args.count)")
        }
        let recv: UInt32 = try toUInt32(receiver)
        let _t = recv.remainderReportingOverflow(dividingBy: try toUInt32(args[0]))
        return .tuple([.int(Int(_t.0)), .bool(_t.1)])
    },
    "func UInt32.remainderReportingOverflow()": .method { receiver, args in
        guard args.count == 1 else {
            throw RuntimeError.invalid("UInt32.remainderReportingOverflow: expected 1 argument(s), got \(args.count)")
        }
        let recv: UInt32 = try toUInt32(receiver)
        let _t = recv.remainderReportingOverflow(dividingBy: try toUInt32(args[0]))
        return .tuple([.int(Int(_t.0)), .bool(_t.1)])
    },
    ]
}
#else
extension FoundationBridges {
    nonisolated(unsafe) static let uInt32: [String: Bridge] = [:]
}
#endif
