// AUTO-GENERATED by BridgeGeneratorTool. Do not edit by hand.
// Regenerate with: bash Tools/regen-foundation-bridge.sh
import Foundation
    import ShellKit
#if canImport(FoundationNetworking)
import FoundationNetworking
#endif

extension FoundationBridges {
    nonisolated(unsafe) static let streamEvent: [String: Bridge] = {
        var d: [String: Bridge] = [
    "init Stream.Event()": .`init` { args in
        guard args.count == 0 else {
            throw RuntimeError.invalid("init Stream.Event(): expected 0 argument(s), got \(args.count)")
        }
        return boxOpaque(Stream.Event(), typeName: "Stream.Event")
    },
    "static let Stream.Event.openCompleted": .staticValue(boxOpaque(Stream.Event.openCompleted, typeName: "Stream.Event")),
    "static let Stream.Event.hasBytesAvailable": .staticValue(boxOpaque(Stream.Event.hasBytesAvailable, typeName: "Stream.Event")),
    "static let Stream.Event.hasSpaceAvailable": .staticValue(boxOpaque(Stream.Event.hasSpaceAvailable, typeName: "Stream.Event")),
    "static let Stream.Event.errorOccurred": .staticValue(boxOpaque(Stream.Event.errorOccurred, typeName: "Stream.Event")),
    "static let Stream.Event.endEncountered": .staticValue(boxOpaque(Stream.Event.endEncountered, typeName: "Stream.Event")),
    "init Stream.Event(rawValue:)": .`init` { args in
        guard args.count == 1 else {
            throw RuntimeError.invalid("init Stream.Event(rawValue:): expected 1 argument(s), got \(args.count)")
        }
        return boxOpaque(Stream.Event(rawValue: try toUInt(args[0])), typeName: "Stream.Event")
    },
        "init Stream.Event(arrayLiteral:)": .`init` { args in
            guard args.count == 1, case .array(let elements) = args[0] else {
                throw RuntimeError.invalid("Stream.Event(arrayLiteral:): expected array literal")
            }
            var result = Stream.Event()
            for element in elements {
                let item: Stream.Event = try unboxOpaque(
                    element, as: Stream.Event.self, typeName: "Stream.Event"
                )
                result.formUnion(item)
            }
            return boxOpaque(result, typeName: "Stream.Event")
        },
        ]
        #if canImport(Darwin)
    d["var Stream.Event.isEmpty: Bool"] = .computed { receiver in
        let recv: Stream.Event = try unboxOpaque(receiver, as: Stream.Event.self, typeName: "Stream.Event")
        return .bool(recv.isEmpty)
    }
        #endif
        return d
    }()
}
